fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
sqlalchemy[asyncio]>=2.0.23
aiosqlite>=0.19.0
asyncpg>=0.29.0
redis>=5.0.0
python-multipart>=0.0.6
python-docx>=1.1.0
PyMuPDF>=1.23.0
sentence-transformers>=2.2.2
rank-bm25>=0.2.2
numpy>=1.24.0
scipy>=1.11.0
langchain-core>=0.1.0
langgraph>=0.0.40
httpx>=0.25.0
python-dotenv>=1.0.0
openai>=1.6.0
tiktoken>=0.5.0
pytest>=7.4.0
pytest-asyncio>=0.23.0

# --- Real RAGAS evaluation (app/evaluation/ragas_real.py) ---
# Judge LLM is our own self-hosted Ollama model (langchain-ollama), and
# embeddings are local sentence-transformers (langchain-huggingface) - no
# hospital data ever reaches a third-party API. Pinned to the 0.2.x/0.3.x
# generation of the langchain/ragas ecosystem: ragas 0.2.15 is incompatible
# with langchain-community>=0.4 (removed modules it still imports). Requires
# Python <=3.12 - ragas's nest_asyncio-based executor is incompatible with
# Python 3.14's stricter asyncio.wait_for/timeout semantics as of this
# writing; on 3.14, /api/evaluation/ragas degrades gracefully to
# "available: false" and the app falls back to ragas_lite (which has no
# such dependency).
ragas==0.2.15
langchain<0.4
langchain-core<0.4
langchain-community<0.4
langchain-ollama<0.4
langchain-huggingface<0.2
