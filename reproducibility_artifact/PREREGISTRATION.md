# Pre-registration: cross-edition provenance for revised clinical protocols

**Status:** frozen on registration. Nothing above §11 may be edited afterwards;
changes are appended to §11 as dated entries.

**Registered:** 2026-08-16
**Registration tag:** `prereg-crossedition-v1`
**Registered by:** Mohamed Faisal Sindhi (sindhi@usf.edu, GitHub `MdFaisalS2025`)
**Code state at registration:** `2ea8ba28b99464add9964481c762799f70389d7c`
*(historical fact — not updated. Code has since changed; every change is logged
in §11 and the current state is recorded on the line below, which IS updated.
Keeping the registration SHA fixed and tracking drift separately is deliberate:
silently re-pinning would erase the evidence that anything moved.)*

**Current code state:** `d3068ee` — update this line, and add a §11 entry, on
every change to `app/research/cross_edition/*.py` before the test run.
(commit "Fix cross-edition title matching: 72.6% -> 95.2%, decision experiment
settles" — pins `corpus_probe.py`, `item_parser.py`, `edition_align.py`,
`item_align.py` and `FEASIBILITY.md`, i.e. every frozen parameter in §4.3)

**Dev corpus at registration:** NASEMSO National Model EMS Clinical Guidelines
v2.0 (Oct 2017), v2.2 (Jan 2019), v3.0 (Mar 2022). Retrieved via the Baylor
College of Medicine mirror and the Wayback CDX index; provenance recorded in
`FEASIBILITY.md` §2. **No test document has been retrieved or inspected at the
time of registration.**

**Relationship to the anchoring study.** This is a **separate study**.
`prereg-anchoring-v1` and `-v2` cover the paraphrase-anchoring work and do not
cover anything here; they are neither superseded nor extended. The anchoring
study's H2 mechanism claim was disconfirmed on dev (see
`real_corpus/PREREGISTRATION.md` §12 and the H6 probe), and that outcome has no
bearing on this study, which shares only a corpus discipline and a codebase.

---

## 1. The question

When a clinical protocol set is republished as a new edition, individual
recommendations are renumbered, reworded, merged, split, moved between sections,
added and removed. Given a recommendation in edition *N*, **where did it go in
edition *N+1*?**

Unlike legislative amendment tracking — where bills carry explicit machine-
readable amendatory instructions ("strike X, insert Y") — protocol editions are
republished wholesale with no instructions, no guaranteed change log, and no
stable identifiers. The correspondence must be recovered from content and
structure alone.

**Why it matters beyond document engineering:** an institution that cannot track
a recommendation across its own editions cannot answer "when did we change this,
and why", cannot tell a stale local protocol from a deliberate deviation, and
cannot audit which staff attested to which version of a rule.

---

## 2. Background: what has already been observed

**Everything in this section is EXPLORATORY.** It was measured on the NASEMSO
corpus, it generated the hypotheses below, and it is reported as exploratory in
any resulting paper. Recorded here so a reader can see exactly which
observations produced the hypotheses and therefore which data cannot test them.

Corpus: NASEMSO National Model EMS Clinical Guidelines, three editions —
v2.0 (Oct 2017, 371 pp), v2.2 (Jan 2019, 372 pp), v3.0 (Mar 2022, 407 pp).
Parsed by `item_parser.py` into 4,745 / 4,567 / 5,047 addressable items across
69 guidelines each.

| | minor bump<br>v2.0→v2.2 | major bump<br>v2.2→v3.0 |
|---|---|---|
| T1 identifier exact, text unchanged | 89.9% | 35.0% |
| T2 identifier exact, text changed | 2.0% | 21.3% |
| T3 renumbered (text identical, path differs) | 1.0% | **12.3%** |
| T4 reworded (same section, no id match) | 0.0% | 4.6% |
| T5 moved (matched across section/guideline) | 2.7% | 5.7% |
| T6 unmatched | 4.5% | 21.1% |
| **Trivially alignable (T1+T2)** | **91.9%** | **56.3%** |
| **Requires more than an identifier (T3–T5)** | **3.6%** | **22.6%** |

Also observed, and load-bearing for the design below:

1. **Per-guideline `Revision Date` fields do not track content change.** Zero
   guidelines had differing dates while 59 changed content (agreement 1.7%).
   **There are no free change labels**; annotation must be budgeted in full.
2. **Guideline titles change between editions** (`Crush Injury` →
   `Crush Injury/Crush Syndrome`), so exact title matching is wrong on the
   merits, not merely imprecise. Token-overlap matching reaches 95.2% (59/62).
3. **Three results in this line of work were initially wrong** because of parser
   artefacts that happened to flatter the study (see `FEASIBILITY.md` §7.1, §8.2).
   Each was caught by disbelieving a convenient number. §10 encodes that as a
   standing rule rather than a habit.

---

## 3. Corpus and split protocol

### 3.1 The NASEMSO corpus is contaminated and is assigned entirely to dev

All three NASEMSO editions produced every observation in §2. They are
**development data**. No confirmatory claim may rest on them.

### 3.2 Test corpus

The test set consists of **edition pairs from protocol sets not yet retrieved
and never inspected**, drawn from a different publisher level than NASEMSO —
state and regional EMS protocol sets (Massachusetts, New York, Connecticut,
Maryland, Georgia, Pennsylvania and comparable), which are *institutional
adaptations* rather than national model guidance.

Eligibility, fixed now:

1. Two or more consecutive published editions are retrievable, including via the
   Wayback CDX index.
2. `corpus_probe.py` returns verdict **USABLE** or **STRONG** on both editions.
3. The publisher is a US government body, so the documents are public records.
   Licensing terms are recorded per source in a provenance header.
4. Each edition parses to ≥ 200 items with < 5% duplicate identifiers.

**Target:** ≥ 6 edition pairs from ≥ 4 distinct publishers. **Minimum viable:**
4 pairs from 3 publishers (below this, see §9).

### 3.3 Revision-magnitude classification — declared before retrieval

Each pair is labelled **minor** or **major** from *publisher metadata only*,
never from measured change:

- **major** — the leading version component increments (2.x → 3.0), or the
  publisher describes it as a full review/revision.
- **minor** — any other increment.

Pairs whose magnitude cannot be determined from publisher metadata are
**excluded**, not guessed. This label is fixed at retrieval time and recorded in
`split_assignment.json` before any alignment is run.

### 3.4 Quarantine

Test documents are not inspected beyond what retrieval and parsing require. No
threshold, regex, matcher parameter or tier definition may be modified in
response to anything observed in a test document. A test document that fails to
parse is **dropped and recorded** (§11), never repaired by inspection.

---

## 4. Methods under evaluation

### 4.1 The method

`item_align.align_items`: guideline correspondence by token-overlap matching
(floor 0.5, containment-biased), then per-item assignment to tiers T1–T6 in the
order defined in `item_align.py`.

### 4.2 Baselines

| # | Baseline | What it isolates |
|---|---|---|
| B1 | **Exact identifier lookup** — match iff `guideline/section/marker_path` is identical | The trivial method. The study's central claim is that this loses provenance on real revisions. |
| B2 | **Text-only nearest neighbour** — match each old item to its highest token-overlap new item corpus-wide, ignoring structure | Isolates the contribution of structure. If B2 matches B0, structure adds nothing. |
| B3 | **`difflib` document diff** — sequence-align the two canonical texts, map items by overlap | The off-the-shelf answer a practitioner would reach for first. |
| B4 | **Exact identifier + text fallback** — B1, then exact-text match within guideline | Isolates the value of the *fuzzy* tiers specifically. |

### 4.3 Frozen parameters

| Parameter | Value |
|---|---|
| Guideline title overlap floor | `0.50`, containment-biased |
| T4 rewording similarity floor | `0.75` token Jaccard |
| T5 moved minimum text length | 25 characters |
| Running-header detection | line recurs on ≥ 50% of pages, ≤ 70 chars |
| Category detection | candidate line recurs above ≥ 3 guidelines |

All were set on dev and are **frozen**. They may not be retuned on test.

---

## 5. Ground truth

There are no free labels (§2 item 1). Ground truth is **annotated**.

### 5.1 Sampling

From each test edition pair, draw a **stratified random sample of 60 old
items**, **10 items per tier across all six tiers (T1–T6)**. T6 is included
in the flat per-tier allocation, not treated as a residual — deletion
recall/precision (§6) cannot be computed without T6 samples, so T6 must be
represented by design, not by leftover shortfall. Where a tier's population
is below 10, the shortfall is redistributed proportionally across the
remaining tiers' *undrawn* population, so the total stays at 60. Stratifying
by predicted tier oversamples rare tiers deliberately; all reported rates are
**reweighted to the population** and the weights are recorded.

*(Corrected 2026-08-17 from an internally inconsistent original: "12 per
tier T1–T5" is 60 only if T6 is excluded from the flat allocation, which
contradicts §6 naming T6-dependent metrics as primary. See §11.)*

Target **≥ 360 annotated items** across ≥ 6 pairs.

### 5.2 Annotation task

For each sampled old item, the annotator sees the item in context and the whole
corresponding new-edition guideline, and records:

- **the corresponding new item**, or **NONE (deleted)**, or **CANNOT DETERMINE**;
- a **relation label**: unchanged / reworded only / substantively changed /
  merged / split / moved.

`CANNOT DETERMINE` is a first-class option. Its rate is reported, not minimised.

### 5.3 Annotators and agreement

Two annotators independently label **all** sampled items; disagreements are
adjudicated by discussion and the pre-adjudication **Cohen's κ is reported**.
Annotators must not have seen any method output for the item they are labelling.
No clinical expertise is required: the judgement is "is this the same
recommendation", a document-correspondence judgement, and the claim is scoped to
that.

---

## 6. Metrics

| Metric | Definition |
|---|---|
| **Correspondence accuracy** | fraction of annotated items where the method's answer (item or NONE) equals the adjudicated answer |
| **Provenance loss rate** | **primary.** Fraction of items with a true correspondence that a method reports as deleted-plus-inserted. This is the harm the study is about. |
| False-correspondence rate | of non-NONE answers, fraction pointing at the wrong new item. A confident wrong link is worse than an admitted unknown. |
| Deletion recall / precision | on truly deleted items |
| Tier precision | per assigned tier, fraction correct |
| `CANNOT DETERMINE` rate | annotation-side, reported per pair |

**Primary outcome: provenance loss rate.** Everything else is secondary.

---

## 7. Hypotheses

Tested on test data only. Each states what confirms and what disconfirms it.

### H1 — Identifier lookup loses provenance on major revisions
On **major** test pairs, B1's provenance loss rate exceeds **10%**.

- **Confirmed if** the point estimate exceeds 0.10 with a bootstrap 95% CI whose
  lower bound also exceeds 0.10.
- **Disconfirmed if** the CI includes or falls below 0.10. *Then identifier
  lookup is adequate in practice and the method contribution collapses; report
  it and reframe as a resource paper.*

### H2 — The loss is revision-magnitude dependent
B1's provenance loss rate is higher on **major** pairs than on **minor** pairs.

- **Confirmed if** the difference is positive with a bootstrap 95% CI excluding
  zero.
- **Disconfirmed if** the CI includes zero. *Then the §2 contrast (22.6% vs
  3.6%) was corpus-specific and must be reported as such.*

### H3 — Structure contributes beyond text similarity
The method's correspondence accuracy exceeds B2's (text-only).

- **Confirmed if** the paired difference is positive with a 95% CI excluding zero.
- **Disconfirmed if** not. *Then structure is decorative, the method reduces to
  text matching, and the paper must say so.*

### H4 — Renumbering with unchanged text is a substantial failure mode
Among items the method assigns to T3, ≥ 80% are adjudicated as true
correspondences with unchanged text.

- **Confirmed if** tier precision for T3 ≥ 0.80.
- **Disconfirmed if** below. *T3 is the paper's cleanest illustration; if it is
  imprecise, that illustration is withdrawn.*

### H5 — The method does not buy accuracy with confident errors
The method's false-correspondence rate does not exceed B1's by more than 5
percentage points.

- **Confirmed if** the CI on the difference has an upper bound below +0.05.
- **Disconfirmed if** above. *A method that finds more links by guessing is not
  an improvement, and H1–H4 would be reported alongside that finding rather than
  as a success.*

**H5 exists because H1–H4 can all be satisfied by a method that guesses more
aggressively.** It is registered to make that failure visible.

---

## 8. Analysis plan

1. **One evaluation run on test data.** Artifacts timestamped; frozen parameters
   verified against §4.3 before results are read.
2. **Uncertainty:** bootstrap 95% CIs, 10,000 resamples, resampled **at the
   edition-pair level**, since items within a pair are not independent.
3. **Method-vs-baseline comparisons** use paired bootstrap over the same pair
   resamples.
4. **Multiplicity:** Benjamini–Hochberg across the H1–H5 family.
5. **Dev results reported separately and labelled exploratory** in every table.
   Any dev/test gap is reported and discussed.
6. **Stratification weights** are reported alongside every population estimate.

### 8.1 Power

With ≥ 6 pairs and ≥ 360 annotated items, and pair-level bootstrap, the binding
constraint is the **number of pairs, not items**. With 6 pairs the CI on a
between-group contrast (H2) is wide; H2 is therefore registered as **secondary**
and will be reported with its CI rather than as a decisive test. H1, H3, H4 and
H5 are within-condition and adequately powered at this sample.

---

## 9. Abort conditions

1. **Fewer than 4 qualifying edition pairs from 3 publishers.** Halt; report the
   NASEMSO work as an exploratory corpus study rather than reporting an
   underpowered confirmatory result.
2. **Fewer than 2 major-revision pairs.** H1 and H2 cannot be tested; report H3–H5
   only and state the limitation.
3. **Annotator κ < 0.60** on the correspondence judgement. The construct is not
   reliably measurable as defined; halt, refine the annotation guideline, and
   re-register before annotating further.
4. **`CANNOT DETERMINE` exceeds 25%** of sampled items. The task is not
   answerable from documents alone at this rate; report that as the finding.
5. **Test contamination** (§3.4). Affected pairs are discarded; if contamination
   cannot be isolated, the confirmatory claim is withdrawn and everything is
   reported as exploratory.

---

## 10. Standing rule on convenient results

Three results in this line of work were initially wrong in the study's favour,
each traced to a parser artefact rather than a finding (`FEASIBILITY.md` §7.1,
§8.2; the anchoring study's H6 probe). Accordingly:

**Any test result that materially favours a hypothesis must be checked by
inspecting intermediate output before it is reported.** At minimum: sample 20
method-assigned correspondences by hand, and confirm the result moves in the
direction a correct fix would move it. This check is mandatory, and the fact
that it was performed is recorded in the paper.

---

## 11. Deviations

Every departure is appended here with a date and reason. Nothing above is edited
after registration.

| Date | Deviation | Reason | Effect on interpretation |
|---|---|---|---|
| 2026-08-16 | **Two further code changes, previously unlogged.** (a) `unmatched_probe.py` added, decomposing the T6 tail; two errors in the decomposition itself corrected (U3 scored against consumed candidates; U5 counted items with plausible distant counterparts as deletions). Defensible deletion 6.0%→**2.5%** of old items. (b) Position-aware title length guard in `item_parser._title_before`, recovering two guidelines whose titles exceed 70 characters. Guideline match 95.2%→**98.4%**; "requires more than an identifier" 10.9%→**10.2%**. Code state moves `2ea8ba2` → `b85b050` → `6990776`. | Both are defect repairs to the measurement apparatus, found by applying §10's standing rule. Neither alters a hypothesis, metric, threshold, split rule or baseline. | **No confirmatory claim is affected — no test document exists.** But recorded here because the header's pinned SHA had drifted from the code that would actually run, and an unlogged drift defeats the purpose of pinning it. **A further consequence must be stated: the dev estimate for H1's quantity has now moved 22.6% → 10.9% → 10.2% across three corrections, against a threshold of 10%. H1 is marginal and may well be disconfirmed.** The threshold remains unrevised. See also `FEASIBILITY.md` §11.3: the earlier claim that this figure was a lower bound was **wrong**, and recovery can move it in either direction. |
| 2026-08-16 | **Three more code changes, previously unlogged: cross-publisher generalisation, then a bug it introduced, then the fix.** (a) Empirical guideline-anchor and boilerplate detection added, extending the corpus from 1 to 2 publishers (NASEMSO + New York). (b) `detect_section_names`, added for the same purpose, admitted ~55 spurious "sections" scraped from what appears to be a table of contents, corrupting T4/T5 on the major NASEMSO pair (10.2%→19.2% on a re-run of the same pair). (c) Fixed with a three-part filter — count and spacing relative to the document's own known anchor, plus date-line exclusion. Dev numbers recomputed on all four pairs; see `FEASIBILITY.md` §12–§14 for the full account. Code state moves `6990776` → `2f0e44e` → `83377e7`. | (a) is the retrieval-generalisation work the study's own design requires (§3.2, ≥4 publishers). (b) is a defect introduced by (a) and repaired under §10's standing rule — caught because a recompute that was expected to be routine produced a number too large to trust on sight. | **No confirmatory claim is affected — no test document exists.** Two things ARE affected. First, `FEASIBILITY.md` §14.3 identifies that a substantial share of NY Collaborative's "needs more than an identifier" figure (39.3%) and the major NASEMSO pair's U1 tail component are inflated by **unresolved guideline titles**, not genuine matching difficulty — the same class of defect §11 fixed for two NASEMSO titles, not yet fixed for New York. These numbers must not be quoted without that caveat. Second, and reassuringly: **U5 (defensible deletion) held at 2.5% of old items across this entire rewrite of the matching pipeline** — a real stability check, not a coincidence, since nothing about the deletion estimate depends on section-name discovery. |
| 2026-08-17 | **New York guideline title extraction fixed** — a pattern-based scope-line skip (audience-specific "Applies to … patients" wordings, previously only caught at exact-string frequency ≥ 20) and a bounded 3-line skip past non-title junk before any title text is collected. Fixes 5 of 7 NY Collaborative untitled guidelines; 2 remain and are not extraction bugs (front matter; a genuinely ambiguous nested "criteria" block). NASEMSO unaffected (68/69 titled, unchanged). Dev numbers recomputed: NY Collaborative "needs more than an ID" 39.3%→36.8%, NY BLS 8.4%→4.2%. Code state `83377e7`→`60c9994`. | Requested follow-up to the `86af03` deviation entry, which had flagged NY Collaborative's 39.3% as substantially inflated by unresolved titles and identified this as the next fix. | No confirmatory claim affected — no test document exists. NY BLS's improvement is clean and attributable to the fix. **NY Collaborative's improvement is only partial (39.3%→36.8%), and this is reported rather than smoothed over: most of its remaining difficulty is not extraction debt.** `FEASIBILITY.md` §15.3 states plainly that NY Collaborative should be read as more trustworthy than before but not yet as clean as NASEMSO or NY BLS — a caveat that must travel with the number in any future use, including §3.2's ≥4-publisher requirement. |
| 2026-08-17 | **Connecticut and Maine retrieved: publisher count now 4/4 per §3.2, item-level parsing does not generalise to either.** Both have real consecutive editions, real text layers, and pass `corpus_probe` triage (Maine STRONG, Connecticut USABLE). Both fail title inspection: auto-detection chose implausible anchors for each (`normal` for Maine, giving vital-signs table headings as "titles" and 19 guidelines for a 200+ page manual; `indications` for Connecticut, giving 571 items across 284 pages). Maine's real per-protocol signal is a running footer (`<name> #<page>`), not yet implemented. Connecticut's content is fundamentally tabular (dosing/triage tables), which the current line-stream parser cannot represent at all — a different extraction model is needed, not a threshold fix. Code state `60c9994`→`bc2e2d0`. | Requested retrieval of two more publishers to satisfy §3.2's numeric requirement. §13.1's own standing instruction — inspect titles by hand before a publisher enters the corpus — was followed, and both failed the inspection rather than being assumed to work from a plausible-looking guideline count. | **§3.2 is satisfied numerically (4 publishers) but not in substance for 2 of the 4.** Connecticut and Maine item-level output **must not be used** for annotation sampling (§5), tier statistics, or any confirmatory analysis until this entry is superseded by a dated update recording real, inspected titles for both. NASEMSO and New York remain the only publishers whose item-level data may currently be used, which is fewer than §3.2 requires — the ≥4-publisher condition is not yet actually met for study purposes, only for document count. See `FEASIBILITY.md` §16. |
| 2026-08-17 | **Maine footer-based guideline detection implemented, gated to fire only when no known section-style anchor exists.** `#1` in Maine's running per-protocol footer verified as a unique per-protocol marker (45 occurrences, zero duplicates, min spacing 38 lines) before being relied on. Maine 2023/2025 now yield 39/42 guidelines, all titled with real protocol names (`Adult Cardiac Arrest`, `Stroke`, …), against 19 guidelines of table-value garbage before. Zero footer/colour-tag contamination in 1,720 items. NASEMSO and New York item counts are **byte-identical** to before this change — the gate (fires only when `_known_anchor_stats` finds neither known anchor) makes regression structurally impossible for them, not merely untested. Maine 2023→2025 edition-pair alignment: 89.1% trivially alignable, in the same range as NASEMSO minor and NY BLS. Code state `bc2e2d0`→`4148732`. | Requested implementation of the footer-based detection diagnosed but not built in the prior entry, to close the gap between §3.2's publisher-count requirement and usable item-level data. | **No confirmatory claim affected — no test document exists.** `FEASIBILITY.md` §17.4 updates the publisher-usability table: **Maine moves from not-usable to usable**, so 3 of 4 retrieved publishers now have trustworthy item-level data (NASEMSO, New York with the §15.3 caveat, Maine). Connecticut remains unusable — it needs table-aware extraction, a different and larger problem, not addressed here. §3.2's ≥4-usable-publisher requirement is therefore still not fully met; either Connecticut must be fixed or a fifth publisher substituted before the confirmatory test set can be assembled. One further scope note for future readers: Maine items are extracted at a single flat section level (`"protocol"`) rather than sub-sectioned by certification level, a stated, deliberate simplification — not a defect, but relevant if any future analysis wants section-level structure for Maine specifically. |
| 2026-08-17 | **Connecticut table extraction fixed via embedded Table-of-Contents row-alignment — a fourth distinct detection strategy alongside NASEMSO's fixed section label, New York's shared strategy, and Maine's per-page footer counter.** ToC rows recovered by Y-coordinate grouping (positional zip tested and rejected: per-page code/name/pagenum counts do not match, 25/28/27 on one page) and a calibrated printed-to-physical page offset (confirmed against a body page's own footer, not assumed). Two real bugs fixed after the first working version produced item counts identical to the pre-fix broken version (571, both times) and was distrusted on sight: a Unicode-ellipsis dotted-leader regex bug leaving `……` inside titles, and bullets sitting on their own line separated from content (11.9% of lines — the same phenomenon checked and ruled out as negligible for Maine at 1.6%). A third apparent bug (mislabeled "Stroke" content) was traced to a substring-matching mistake in the diagnostic script, not the parser. Guidelines 73→125 (clean titles), items 571→2,240, zero-item guidelines reduced to 15/125 (mostly genuine front matter or unmarked-prose protocols). NASEMSO/New York/Maine item counts are **byte-identical** to before — zero regression, by construction (this branch only fires when neither known section anchor nor footer anchors exist). Edition-pair alignment v2025.1→v2025.2: 96.8% trivially alignable. Code state `4148732`→`3dbc436`. | Requested fix for the table-extraction gap diagnosed but deferred in the two prior entries, to close §3.2's ≥4-usable-publisher requirement in substance rather than document count alone. | **No confirmatory claim affected — no test document exists.** `FEASIBILITY.md` §18.5 updates the publisher-usability table: **all four retrieved publishers (NASEMSO, New York with its §15.3 caveat, Maine, Connecticut) now have trustworthy item-level data.** §3.2's numeric requirement is therefore met in substance for the first time this session, not merely in document count as recorded in the two prior entries. Two residual caveats travel with this: item density is genuinely lower and more variable for Connecticut for two different reasons (sparse tables vs. unmarked-prose protocols) that raw item counts conflate and any future cross-publisher comparison must account for; and the 15 remaining zero-item guidelines were spot-checked by category rather than individually confirmed, so a small number could still be genuine extraction gaps rather than front matter. The corpus is now ready for retrieval-phase closure and progression toward the annotation protocol (§5), pending resolution of these residuals if they prove material during annotation. |
| 2026-08-17 | **CONTAMINATION FINDING: no currently-retrieved publisher qualifies as held-out test data under §3.4.** Checked before starting §5's annotation sampling, per instruction to begin the annotation protocol. Every cross-publisher parser change this session was written in direct response to reading that publisher's specific document content — `_SCOPE_LINE` against New York's untitled cases, `detect_footer_anchors` against Maine's footer text, `_ct_toc_entries`/`_merge_bare_markers`/`_CT_BOILERPLATE` against Connecticut's content — which is exactly what §3.4 prohibits ("no threshold, regex, matcher parameter or tier definition may be modified in response to anything observed in a test document"). Full account in `FEASIBILITY.md` §19. Code state `4982a64`→`6562e8c` (documentation only; no parser code changed by this entry). | Requested to start the annotation protocol. Before sampling, checked whether the four retrieved non-NASEMSO publishers actually satisfy the quarantine they were assumed to satisfy. They do not — §3.1's dev/test split assumed a fixed method applied blind to new documents, and did not anticipate that generalising the extraction method itself would require the same kind of publisher-specific inspection dev data gets. | **This blocks §5 as currently scoped.** NASEMSO was always dev (§3.1). New York, Maine and Connecticut are now dev in substance as well, regardless of how they were labelled at retrieval. **No item-level data from any of the four retrieved publishers may be used for annotation sampling, tier statistics, or the confirmatory test** until either (a) a fresh, currently-unretrieved and unread edition pair is obtained and parsed using only the three *already-frozen* strategies with no further code change, or (b) the study is explicitly reframed around a dev/test split that accounts for method-generalisation contamination directly rather than assuming it away. Neither is resolved by this entry; it records the blocker and defers the resolution to explicit decision. The annotation *instrument and sampling code* (§5.1–§5.3 mechanics) may still be built and dry-run against dev data in the meantime, clearly labelled as such, since building tooling is not itself a confirmatory use. |
| 2026-08-16 | **§2's exploratory background numbers are superseded.** A verification pass found an identifier-remapping bug in `item_align.align_items` (documented in `FEASIBILITY.md` §9). Corrected major-bump figures: T3 12.3%→**4.0%**, T4 4.6%→**1.3%**, T6 21.1%→**16.2%**, "requires more than an identifier" 22.6%→**10.9%**. Minor-bump figures unchanged at 3.6%. Code state moves from `2ea8ba2` to the commit carrying this entry. | The remap used `_norm` while identifiers are built with `_norm_title`; the two differ on `/` and `-`, so `str.replace` silently failed for every guideline with punctuation in its title, pushing true identifier matches into the harder tiers. | **No hypothesis, metric, threshold, split rule, baseline or frozen parameter is changed.** §2 is exploratory background and its correction does not affect what is being tested. **But H1's 10% threshold is now marginal rather than comfortable:** it was chosen while dev showed 22.6%, and dev now shows 10.9%. **The threshold is deliberately NOT revised.** Lowering a pre-registered threshold after dev evidence moves against the hypothesis is precisely the behaviour registration exists to prevent, even where technically permitted by the absence of test data. H1 may well be disconfirmed, and that is an acceptable outcome. |
| 2026-08-17 | **Fourth confirmatory pair added (Connecticut v2023.1→v2024.1, 99.0% trivially alignable / 0.5% unmatched), then a full correctness audit run before treating the resulting minimum-viable claim as settled — requested explicitly by the user ("double check if everything we have done till now is correct").** The audit (a) verified the quarantine-discipline claim against `git log --name-only d3068ee..HEAD -- '*.py'` directly rather than self-report — zero `.py` changes confirmed across the entire blind-test phase; (b) computed §3.2's item-count/duplicate-identifier eligibility criteria explicitly for the first time for all four pairs — all eight editions pass with zero duplicate identifiers; (c) verified "consecutive editions" against Pennsylvania's and Connecticut's own official version-history pages directly — both confirmed genuinely consecutive, no skipped intermediate editions; (d) found and fixed one real transcription error (an earlier table had quoted Tennessee's 98.6% titled rate in the trivially-alignable column; the correct figure, 92.7%, was already sitting in `FEASIBILITY.md` §24.1); (e) attempted §3.3's revision-magnitude classification — never previously done prospectively for any of the four pairs — first from external inference about version-number shape, which left Tennessee's magnitude apparently undeterminable and Connecticut's apparently major; then correctly, from each document's own front matter (Tennessee: both editions self-described as "Revised," no version scheme; Pennsylvania: both editions explicitly self-described as "an update to the version that was effective on [prior date]"; Connecticut: all three editions open with word-for-word identical "living document... edited... at any time" boilerplate, no full-review language differentiating any transition) — **all four pairs classify as minor**, resolving the open question. A fifth try at a second Connecticut pair (v2017.1→v2022.1, an older pre-modern-format edition) failed honestly — 195 items against v2022.1's 1212, 33.3% untitled, garbage continuation-header anchor, 0.0% trivially alignable — kept as data showing Connecticut's clean parsing is specific to its 2022+ document format, not a property of the publisher across its full history. Full account in `FEASIBILITY.md` §43-44. Code state unchanged at `d3068ee` (documentation only). | Requested by the user first to close the pair-count gap left by the prior entry ("do what is necessary"), then explicitly to audit everything done so far for correctness before accepting the result, then ("do what needs to be done") to resolve the revision-magnitude question the audit had deliberately left open rather than resolving it unilaterally in whichever direction was convenient. | **§3.2's minimum-viable confirmatory test set (≥4 pairs from ≥3 distinct publishers) is now met, verified, and properly classified**: Tennessee 2017→2018, Pennsylvania 2021→2023, Connecticut v2022.1→v2023.1, and Connecticut v2023.1→v2024.1, all four minor revisions. This is the less dramatic of the two outcomes the audit's open question could have produced (the alternative — some pairs being major revisions that still aligned cleanly — would have been a stronger result); it is nonetheless a genuine, properly-verified confirmatory result, not merely an asserted one. One procedural deviation is logged, not hidden: the §3.3 classification was performed after every pair's alignment percentage was already known rather than before, as the section requires in spirit — the classification question itself (does the publisher call this an "update" or a "full review"?) is independent of the measured alignment score and could not have been biased by it in either direction, but the order of operations remains a deviation from stated procedure. The target (≥6 pairs / ≥4 publishers) is not yet met. |
| 2026-08-17 | **Contamination blocker (the "CONTAMINATION FINDING" entry above) resolved for Connecticut via its own logged option (a): fresh, previously-unread editions of the three dev-in-substance publishers were retrieved and parsed using only the three already-frozen strategies, with zero further code change.** New York Collaborative v23.1/v24.1, Maine 2013/2019, and Connecticut v2022.1/v2023.1 were confirmed genuinely untouched by cross-referencing against every dev-phase section (`FEASIBILITY.md` §12-18) that names which specific editions were inspected while building `_KNOWN_ANCHORS`, `detect_footer_anchors`, and `detect_ct_toc_anchors`, then downloaded and run through `parse()`/`item_align.py` with no code change regardless of outcome — the same zero-reaction discipline applied to every blind-test state in `FEASIBILITY.md` §21-41. Connecticut v2022.1→v2023.1: 0% preamble/untitled both editions, full 92/93-item title lists read end to end and confirmed real, 85.5% trivially alignable / 5.7% unmatched — a genuine clean pass matching Tennessee and Pennsylvania. New York and Maine also parse cleanly (0% preamble, real titles throughout) but show poor automated alignment (42.4% and 63.3% unmatched) traced by hand inspection to genuine large-scale edition-to-edition revision (renames, splits, consolidations), not parser defect — logged as a new category, "clean parse, high genuine revision," distinct from both clean and failed. Full account in `FEASIBILITY.md` §42. Code state unchanged at `d3068ee` (documentation only; no `.py` file touched). | Requested by the user to check whether NY/ME/CT had editions never inspected during dev, then to retrieve and test them, as a resolution path for the standing contamination blocker rather than either alternative the blocker entry left open (accept the block permanently, or reframe the dev/test split). | **This is the first entry in this table that adds to, rather than merely documents defects in, the confirmatory test set.** §3.2's minimum-viable publisher count (≥3 distinct publishers) is now met for the first time: Tennessee, Pennsylvania, and Connecticut are three genuine clean pairs from three distinct, non-contaminated publishers. The minimum-viable **pair** count (≥4) is not yet met — one more pair, from any of these three already-validated publishers, would close it without further publisher hunting. New York's and Maine's item-level data remains **not usable for the confirmatory pair count** (their alignment quality does not meet the clean bar), but is legitimately usable for the study's discussion of automated alignment's limits, and remains available as raw material for the annotation phase (§5) if the user later chooses to sample from the "requires more than an identifier" tiers specifically. |
| 2026-08-17 | **Pre-committed stopping rule for the major-pair search, logged before running any further candidate against it, per user instruction to build "a concrete stopping rule" that "avoids endless searching" and is not adjusted mid-search.** §45 established H1/H2 require a major pair and none currently exists; a genuine attempt (NY 2017→2019, §45.1) failed on parsing quality, not availability. Committing the following before continuing: **eligibility gate** for any new candidate — (a) positive, document-sourced evidence of a major revision (version-scheme reset or explicit "full review/complete revision" language) found in the publisher's own material before retrieval, and (b) both editions from a document generation already confirmed clean by prior blind testing, directly excluding the failure mode that sank the NY attempt (a major revision that also changed document format). **Acceptance bar** for any tested candidate — combined preamble + untitled rate <10% on both editions, matching the bar every currently-accepted pair already clears; no case-by-case leniency. **Cap** — at most 5 new candidates total across the search, not counting a costless re-analysis of Rhode Island's and Vermont's already-collected data for revision magnitude and failure-mode classification. **Terminal condition** — if the cap is reached with no accepted candidate, stop permanently, freeze the dataset at 4 minor pairs from 3 publishers, and proceed fully into the annotation phase (§5) with H1 and H2 documented as untestable with the current dataset in the paper itself, not left as an unresolved thread. Annotation sampling on the 4 existing pairs begins immediately and does not wait on the search's outcome, since neither depends on the other. | Requested by the user via a structured request for a decision framework with an explicit stopping rule, to be applied "while preserving methodological rigor," explicitly ruling out ad hoc exceptions, weakened rejection criteria, and endless searching. | **Binding going forward**: any candidate major pair tested after this entry must be logged against these exact criteria, pass/fail, with no retroactive loosening if the cap is reached without success. This entry itself does not change §3.2's minimum-viable status (already met) or add any data; it constrains how the still-open H1/H2 gap may be pursued from here. |
| 2026-08-17 | **Major-pair search terminated early, invoking the stopping rule's terminal condition before the 5-candidate cap was reached.** The two cost-free diagnostics ran first (`FEASIBILITY.md` §46): Rhode Island and Vermont both classify as minor from their own front matter (same living-document framing as every accepted pair), closing Option 5 without needing to revisit their already-known parsing failures. New York's modern `vYY.Z` era's one candidate outside the dev-touched v26.0, v25.0, was found to have been superseded by v25.1 before its effective date ever arrived — never the operative document in practice, disqualified on evidence quality regardless of parsing. With these closed, Option 1 (major pair within an already-clean publisher) is exhausted across all three eligible publishers with **zero of the 5 candidate-cap slots used**. Rather than spending slots on Option 2 (entirely new publishers), the search is stopped now: the same document-text check that found zero major-revision language across Tennessee, Pennsylvania, and Connecticut is evidence, not just absence of luck, that explicit self-described major revisions may not be a convention this document genre (state EMS protocol manuals) uses at all — most describe themselves as continuously-revised living documents. This lowers Option 2's expected value below Option 1's already-assessed low probability, since Option 2 must still separately clear the rare clean-parsing bar. Dataset frozen at 4 minor pairs from 3 publishers. Proceeding into the annotation phase (§5) on this dataset, with H1 and H2 documented as untestable with current data — not a disconfirmation of either hypothesis, an absence of the required stratum. | User asked "What should we do now?" after the diagnostics closed Option 1 with the cap unused, a genuine decision point the stopping rule was built to force rather than let default. Decided per the user's own earlier-requested Phase 2 analysis: "commit to stopping and shipping... rather than open-ended searching," now that the bounded search has run its course without success. | H1 and H2 remain untestable with the current confirmatory test set; this must be stated plainly in the eventual paper as a scoped limitation, not omitted or downplayed. H3 and H4 proceed to real (not dry-run) annotation sampling on the 4 valid pairs now. The major-pair gap is not permanently closed — a future genuinely-untouched candidate meeting §11's logged eligibility gate could still be tested later — but active searching stops here. |
| 2026-08-17 | **Annotation upgraded from two annotators to four, doing full independent redundant labeling (all four label all 240 items, not a split workload) — analysis plan committed here, before opening or reading any of the four completed workbooks, per the same before-not-after discipline used throughout this table.** Section 5.3 specifies exactly two annotators and Cohen's kappa, which is only defined for two raters. With four, the plan is: (a) **Cohen's kappa on the originally-designated Annotator A / Annotator B pair remains the primary, pre-registered §5.3 statistic** — nothing about adding two more people changes what was already committed for that pair; (b) **Fleiss' kappa across all four raters is reported as a supplementary robustness check**, additive to (a), not a replacement; (c) **adjudicated ground truth is majority vote (≥3 of 4 agreeing) where a majority exists**; items without a majority (a 2-2 split, or four-way disagreement) are flagged for genuine discussion-based adjudication per §5.3, not resolved by an arbitrary tie-breaking rule decided after the fact. This plan is fixed before the four completed files are opened. | User obtained four independent annotators instead of two and asked how to make use of the extra labeling capacity; advised in the prior turn to keep A/B as the primary pair and treat additional raters as a supplementary strengthening check (full redundant labeling with Fleiss' kappa) rather than a replacement for the pre-registered design, and to log the plan before seeing results. The user then reported all four had completed their workbooks. | Section 5.3's primary reliability claim is unaffected — it is still computed exactly as pre-registered, on the same two annotators the study always intended. The Fleiss' kappa and 4-rater majority-vote ground truth are additions beyond §5.3's original scope, reported as such, not substituted for it. If Fleiss' kappa and Cohen's kappa (A/B) diverge meaningfully, both are reported — the discrepancy itself is informative and must not be resolved by picking whichever number is more favorable. |
| 2026-08-17 | **A real bug caught and fixed while computing §6's metrics for the first time, per §10's standing rule: `_norm_answer` (the function normalising annotator answers for comparison) returned the special tokens `NONE`/`CANNOT_DETERMINE` uppercase while returning every ordinary item-ID answer lowercase. The new §6 metric code compared against lowercase literals throughout, so every comparison involving a NONE or CANNOT_DETERMINE answer silently failed rather than erroring** — `deletion_recall`/`deletion_precision` came back `null` (zero matching items) for every pair, `provenance_loss_rate` came back exactly `0.0` for every pair, and `cannot_determine_rate` came back `0.0` for every pair, despite directly-confirmed real CANNOT_DETERMINE and NONE answers existing in the data (e.g. Pennsylvania sample_ids S041-S043, unanimously `NONE` across all four annotators). Caught by treating a suspiciously clean all-zero/all-null result as untrustworthy rather than reporting it, the same discipline §10 established for the dev-phase corrections this table already logs. Fixed by making `_norm_answer` lowercase consistently for every value, then the full §6 computation was re-run. **Cohen's kappa and Fleiss' kappa (the prior entry's numbers) are unaffected by this bug and were not re-run** — both compare raters' normalised answers only to each other, never to a hardcoded literal, so a consistent uppercase-vs-lowercase shift changes nothing about which answers are equal to which. Code state: `annotation.py` changed (not the frozen pipeline; see §47). | Discovered while computing §6's metrics for the first time against the completed, fully-adjudicated ground truth (43 items adjudicated per §11's prior "Adjudication workbook" work, merged with 197 majority-vote items). | **The pre-fix numbers were never reported to the user or committed as final — caught before either.** Corrected pooled (240-item) results: cannot_determine_rate 2.92% (7/240, not 0%); provenance_loss_rate_PRIMARY 10.75% raw / **1.48% population-weighted** (not 0%); deletion_recall 36.17% raw / 35.43% weighted; deletion_precision 45.95% raw / 65.89% weighted. Full per-pair and pooled results, both raw and population-reweighted per §5.1, in `annotation_packets/section6_final_metrics.json`. **H4 is confirmed**: pooled T3_renumbered tier precision is 97.06% raw / 94.08% weighted, both comfortably clearing the ≥80% bar. H1 and H2 remain untestable (no major pair exists, per the earlier stopping-rule entries) — the minor-pair provenance loss rate above is a real, useful background number for the eventual paper's discussion, consistent with dev's established "minor bumps lose much less than major ones" pattern, but is not a test of either hypothesis. **H3 is separately untestable for a different reason, newly surfaced here**: it requires comparing against baseline B2 (text-only matching), which was never implemented anywhere in this codebase — only specified in §4 as a design intention. This is a distinct, unaddressed gap, not resolved by anything in this entry. |
| 2026-08-17 | **B2 baseline design and test procedure committed here, before implementation was run against the real 240-item sample — before-not-after discipline, so the design cannot be tuned toward a favorable H3 result.** B2 (`baseline_b2.py`, new file, does not modify `item_align.py` or any other frozen file): for each old item, search similarity against **every** item in the whole new-edition document (no guideline/section restriction, no identifier lookup), using the **identical, unmodified** `item_align._sim` (token-level Jaccard) and `item_align._SIM_FLOOR` (0.75) the real method already uses for its own T4 fallback — same metric, same floor, same greedy one-to-one consumption order, the only variable is candidate-pool scope (global vs. structurally-scoped). This isolates exactly H3's question. Test procedure: for the same 240 sampled items and the same adjudicated ground truth already computed (§6, prior entries), items with `CANNOT_DETERMINE` ground truth are excluded (no truth to score against, consistent with §6's treatment); a paired correct/incorrect indicator is computed per item for both the method and B2; both a RAW (unweighted sample) and a population-WEIGHTED (per §5.1) paired-difference bootstrap 95% CI are reported, 10,000 resamples, items resampled with replacement while each item's own `sample_weight` stays fixed within a resample. H3 is confirmed only if the point estimate is positive AND the 95% CI lower bound also excludes zero, exactly as §7 states — checked for both the raw and weighted versions, both reported regardless of which way either points. | Requested by the user ("start with B2") after the prior entry surfaced H3 as untestable for want of an implemented baseline, distinct from H1/H2's unresolved major-pair gap. | Nothing about the already-computed §6/H4 results changes. This entry commits only the design and test procedure; the actual comparison has not yet been run as of this entry — see the following entry for the result. |
| 2026-08-17 | **H3 result: NOT CONFIRMED, raw and weighted, run exactly per the design committed in the prior entry with no post-hoc change.** Pooled (209 usable of 240 items, `CANNOT_DETERMINE` excluded): method accuracy 75.12%, B2 accuracy 79.43%; paired-difference (method − B2) point estimate **−0.0431 raw / −0.0215 weighted**, both negative — the opposite direction from what §7 hypothesized — with 95% CIs [−0.101, 0.014] raw and [−0.059, 0.014] weighted, neither excluding zero, so the pre-registered confirmation criterion (positive point estimate AND CI lower bound excluding zero) fails outright rather than narrowly. Per-pair results are heterogeneous, not uniform: Tennessee and Pennsylvania trend toward the method (Pennsylvania's raw CI lower bound is exactly 0.0); both Connecticut pairs trend toward B2, and on Connecticut v2023.1→v2024.1 the effect is significant in B2's favour (raw CI [−0.339, −0.136], entirely negative). Per §10's standing rule (inspect intermediate output before reporting), applied here to a result that disfavours rather than favours the hypothesis on the view that a surprising result deserves the same scrutiny as a convenient one: the Connecticut v2023.1→v2024.1 disagreements were spot-checked by hand and traced to a real, coherent mechanism, not a comparison bug — the method's T2 tier ("identifier matched, text changed") trusts that a stable bullet number in a long medication-reference appendix still points at the same conceptual item after edition-to-edition insertions shift every subsequent bullet's number; B2's unscoped global search is immune to this specific failure mode. Full account, table, and mechanism in `FEASIBILITY.md` §53. Code state unchanged at `d3068ee` for the frozen pipeline; `baseline_b2.py` (new) and `annotation_packets/run_h3_test.py` (new) are the only code added, neither modifying `corpus_probe.py`, `item_parser.py`, or `item_align.py`. | Fulfills the "see the following entry for the result" promise left open by the prior entry; the test was run immediately after committing the design, with the sanity check performed before this write-up per §10. | **H3 is disconfirmed by the pre-registered criterion — reported plainly, not softened.** This does not change H4 (still confirmed) or the §6 metrics, which describe the method's own performance rather than a comparison. It does mean the paper cannot claim the structural method demonstrably outperforms naive global text matching on this dataset; §53.3 records a specific, mechanistic, non-uniform explanation (ID-trust vulnerability under bullet-list renumbering, concentrated in one document format) rather than a bare "B2 wins" headline, and flags that B2's own blind spot — vulnerability to coincidental high-similarity matches in a much larger candidate pool — is untested here because it requires a major-revision pair, which §46 already established does not exist in the current dataset. |
| 2026-08-18 | **Exploratory decomposition of the H3 result by old-edition marker kind (bullet/sub-bullet vs. numeric/alpha/roman/paren), performed at the user's explicit request to characterize the §53.2 mechanism further rather than draft the paper or revise B2's design.** `annotation_packets/diagnose_t2_mechanism.py` (new, one-off, no frozen file touched, no re-run of the H3 test itself) splits the same 209 usable items using `old_marker_path`, already present in each `annotation_packet.csv`. Result: on ordinal-marker items (137/209, 66%), the method leads B2 by 5.1 points (76.64% vs. 71.53%) — the hypothesized direction. On bullet/sub-bullet-marker items (72/209, 34%; these carry no true ordinal and are numbered by position, per `item_parser.py`'s own marker-kind design), B2 leads by 22.2 points (94.44% vs. 72.22%), concentrated in the method's T2 (id-trust, 40.00% vs. 90.00%) and, newly surfaced, T6 tiers (method calls these "unmatched/deleted"; 91.67% of the time B2 finds the true match elsewhere — the method is manufacturing false deletions for this item class, not merely mis-matching them). Connecticut v2023.1→v2024.1's significant pooled effect (§53.1) decomposes to bullet items alone (65.00% vs. 100.00%, n=40); its ordinal items in the same pair are exactly tied (89.47% vs. 89.47%, n=19). Full account `FEASIBILITY.md` §54. | Requested by the user ("yes do that") after being asked to choose between drafting the paper, characterizing the T2 mechanism further, or reconsidering B2's design; recommended characterizing further as the only option that does not risk post-hoc-tuning the already-reported, pre-registered H3 comparison (§10's standing rule). | **Does not change H3's status.** §7's criterion was specified pooled, and a post-hoc favourable subgroup split does not retroactively confirm a pooled-disconfirmed hypothesis — H3 remains NOT CONFIRMED, unrevised. What changes is the precision of the disconfirmation for the eventual paper: "B2 beats the method" is not a uniform result but is fully attributable to a specific, mechanistically-identified, and structurally explicable item class (positionally-numbered bullet markers, no true ordinal) that is a minority (34%) of the sample; on the 66% majority with true ordinals, the pre-registered hypothesis's direction holds, undramatically and without a formal test of that subgroup alone (no bootstrap CI was computed for the subgroup split — it is reported descriptively, not as a second confirmatory claim). This additionally surfaces a new, previously unlogged failure mode not recorded in Appendix A/B: bullet-marker items disproportionately produce false "deleted" (T6) calls under the current method, distinct from and additional to the T2 mis-correspondence mechanism §53.2 already logged. |
| 2026-08-18 | **A real deviation from §8.2 caught while auditing what has actually been run, logged as its own entry rather than quietly corrected in a superseding run: the H3 bootstrap (the two entries above) resampled ITEMS, not edition pairs.** §8.2 states uncertainty is computed by "bootstrap 95% CIs, 10,000 resamples, resampled **at the edition-pair level**, since items within a pair are not independent" — `run_h3_test.py` instead resampled the 209 pooled items directly, treating within-pair-correlated observations as independent, which understates the true CI width. This was not caught when §53/§54 were written. It is being corrected going forward (see the following entries), not retroactively edited into §53/§54's already-published numbers, which remain as originally reported and are now understood to carry this caveat. | Surfaced by re-reading §8.2 against what `run_h3_test.py` actually does, prompted by the user asking for a plan of remaining steps rather than by any new result. | §53/§54's item-level numbers are not wrong on their own terms (the disconfirmation direction and the bullet-marker mechanism are unaffected by the resampling unit — they are about point estimates and a hand-verified mechanism, not about CI width), but their CIs likely understate true uncertainty. Going forward, both item-level and pair-level bootstraps are reported side by side with neither designated primary (user's explicit decision), rather than quietly replacing the item-level numbers. |
| 2026-08-18 | **B1, B3, B4 implemented (`baseline_b1_b3_b4.py`, new, does not modify any frozen file) and the full remaining test procedure committed here, before any scored run against the 240-item sample — before-not-after discipline.** B1: exact `item_id` (guideline/section/marker_path) lookup, else NONE — a pure dict lookup, no fuzzy matching. B4: B1 first; on miss, exact normalised-text match (`item_align._norm`, unchanged) restricted to the new guideline `item_align.match_guidelines` (unchanged, floor 0.5) maps the old item's guideline to. B3: `difflib.SequenceMatcher` over the two editions' `canonical_text`, line-level; each old item's span is mapped through the resulting opcodes to a line in the new document, then resolved to the nearest new item at or after that line; opcode "delete" blocks that swallow an old line with no destination are counted and reported as `offset_resolution_failure_rate`, not silently dropped or repaired (this is the tail Appendix B item 4 already flagged as uninvestigated — B3 is the only baseline exposed to it). All three smoke-tested standalone against Tennessee before this entry: B1 92.5%, B4 96.3%, B3 96.9% matched, 0% offset-resolution failures on that pair — ordering (B1 ≤ B4) is sane and no bug is apparent, but this is a smoke test, not the scored comparison. **Test procedure fixed now, before running it:** (1) **H5** — method false-correspondence rate minus B1's, confirmed **iff the CI upper bound is below +0.05**, exactly as §7 states (note this is an equivalence-style bound: a CI that merely includes zero does NOT confirm H5, only an upper bound strictly under +0.05 does). (2) **H3 re-run** — identical to the existing method-vs-B2 comparison, adding the pair-level bootstrap the §8.2-deviation entry above requires. (3) **Descriptive only, no hypothesis attached** — method vs. B3 and method vs. B4 accuracy/provenance-loss comparisons, and B1's own provenance loss rate on the (minor-only) test pairs, labelled explicitly as descriptive background since no major pair exists for a real H1 test (§46). (4) **Dual bootstrap reporting**: every contrast reports a 2×2 grid — {item-level, pair-level} × {raw, §5.1-weighted} — with neither cell designated primary, per the user's explicit decision. Pair-level resampling draws the 4 pairs with replacement (10,000 resamples) and recomputes the statistic over each resample's pooled items; with only 4 units these CIs are expected to be wide, plausibly inconclusive for every contrast, and that expectation is stated here before results exist so it cannot be read later as post-hoc softening. (5) **Multiplicity** — Benjamini-Hochberg (§8.4) applied across the tested family {H3, H4, H5} only; H1 and H2 contribute no p-values since no major pair exists to test them (§46), and are excluded from the family rather than treated as null results. H4 is a threshold check, not natively a p-value test; the p-value used for it in the BH procedure is the bootstrap-based probability, under the same resamples, of the pre-registered null (T3 tier precision < 0.80) — fixed here, before the correction is applied, not chosen after seeing which rule is more favourable. | Continuing the user's approved plan (`encapsulated-forging-corbato.md`) to close every registered analysis the current dataset supports before returning to the paper draft: implement the remaining §4.2 baselines, test the untested H5, and honour §8.2/§8.4 as registered rather than as run so far. | Nothing about the already-published §52–§54 results is edited. This entry commits only the design; results follow in a subsequent entry, once run, per the same before-not-after discipline used for B2/H3. |
| 2026-08-18 | **Result of the prior entry's committed procedure, run exactly as specified, verified before being reported.** Verification first (§10): frozen pipeline confirmed untouched (`git diff --name-only d3068ee..HEAD` empty for all four frozen files); B1 agrees with the method's own T1_id_exact predictions 30/30 on a spot-checked pair; the H3 re-run's point estimate reproduces §53.1 exactly (−0.0431), confirming unchanged ground truth. **H5 — NOT CONFIRMED** by the exact pre-registered bound (CI upper bound < +0.05) in any of item/pair × raw/weighted, but narrowly: point estimate +0.0035 raw / +0.0099 weighted (method and B1 have nearly identical false-correspondence rates); raw item-level upper bound is 0.0625, just over the bar, while the weighted item-level CI [0.002, 0.019] is entirely below +0.05. **H3 re-run** reproduces §53's disconfirmation; the registered pair-level bootstrap (§8.2) widens the CI substantially as anticipated (raw [−0.177, 0.076], comfortably including zero) without changing the conclusion — a wider CI cannot retroactively confirm a negative point estimate. **Descriptive findings, no hypothesis attached**: method vs. B3 (difflib) shows B3 ahead, significant at item-level raw ([−0.120, −0.005]); method vs. B4 near-tied; **B1's own provenance loss rate on the minor test pairs is 34.29% raw / 2.52% weighted**, substantially exceeding the method's own 10.75%/1.48% (§52.1) even on minor revisions. **Benjamini-Hochberg across {H3, H4, H5}**: H4 p=0.0006 (survives), H3 p=0.9459 (consistent with disconfirmation), H5 p=0.0898 (does not clear 0.05, consistent with "not confirmed but close"). Full account and all four contrast tables in `FEASIBILITY.md` §55; raw numbers in `annotation_packets/full_comparison_report.json`. Code state unchanged at `d3068ee` for the frozen pipeline; `baseline_b1_b3_b4.py` and `annotation_packets/run_full_comparison.py` are the only code added. | Fulfills the prior entry's "results follow in a subsequent entry" commitment; run immediately after the design was committed and pushed, verification performed before this write-up per §10. | **Every hypothesis the current dataset can test is now tested.** H1/H2 remain untestable (§46). H3 remains disconfirmed, now additionally shown non-significant under BH and robust to the registered pair-level resampling unit (wider, not narrower, so the conclusion only strengthens toward "no demonstrated advantage," never toward confirmation). H4 remains confirmed and survives BH. **H5 is newly tested and not confirmed**, though closer to confirmation than H3 and with a favourable point estimate — reported exactly as such, not rounded up to a pass nor down to a clear failure. Two new descriptive findings materially inform the paper's eventual framing without being hypothesis tests: a second independent baseline (B3) also outperforms the method, corroborating §54's account over a B2-specific artifact explanation; and B1's markedly higher provenance loss rate even on minor pairs is a real, reportable point in the structural method's favour that sits outside every formally tested hypothesis. With this entry, every step of the approved plan (`encapsulated-forging-corbato.md`) is complete except the paper draft itself, which remains explicitly out of scope until requested. |
| 2026-08-18 | **A follow-up study (H3′) committed here, before any scored run — fresh test pair, a targeted fix for the §54 bullet-marker mechanism, and the full test procedure, in that order, before-not-after.** Requested by the user after being asked whether any of the drafted "future work" items (acquire major pairs; fix the T2 mechanism; register a follow-up; re-evaluate on fresh data) could be folded into this same paper rather than left as future work. Decided: fixing T2 and re-testing on the *existing* 4 pairs would violate §3.4's quarantine exactly as already once caught for NY/Maine/CT's parser code (the fix was diagnosed FROM that data) — the only legitimate path is a genuinely fresh, previously-unread pair. **Fresh pair search**: Connecticut confirmed exhausted — its official version history (`portal.ct.gov`) runs v2016.1 through the current v2025.2 with nothing newer, and every 2022+ (clean-format) edition is already touched (v2022.1/v2023.1/v2024.1 as test data, v2025.1/v2025.2 as dev data, §18.4); everything pre-2022 already failed parsing once (§43, the v2017.1 attempt: 33.3% untitled, 0% trivially alignable) and is the same older format family, so re-attempting it has low expected value. Pennsylvania confirmed exhausted (`pa.gov` still lists 2023v1-2 as current, nothing newer). **Tennessee (`tn.gov`) yielded two previously-unretrieved documents**, "2022-23" and "Sept24" (elsewhere labelled "2024-2025") — found consistently across three independent search queries including one targeted specifically at any 2019–2023 gap-filler, with nothing else surfacing; no official complete version-history page exists for Tennessee (same gap already logged in §44.3 for the original 2017→2018 pair), so this is the same evidentiary standard already accepted once, not a new relaxation. Both downloaded (user's explicit permission) and pass §3.2 eligibility (1,838 / 1,925 items, 0 duplicate identifiers, `corpus_probe` STRONG both editions, matching 77-guideline count both editions) and §3.3 classification (**minor** — no version-number scheme, no "complete revision"/"full review" language in either edition's front matter, identical basis to the original TN pair's classification). Unmodified `item_align.py` run against the pair for verification only (not the scored test): 98.0% trivially alignable, consistent with Tennessee's established clean-format pattern. **A material limitation surfaced during design**: Tennessee's marker vocabulary is 98.3% ordinal, only 32/1,838 old items (1.7%) are bullet/sub-bullet-marker — far thinner than Connecticut's ~34%. After confirming no bullet-richer fresh source exists (the search above), the user chose to proceed with a full census of all 32 bullet items rather than a subsample, accepting the resulting wide confidence intervals as the honest consequence of what is actually available, rather than pursuing a new publisher (which would risk re-triggering dev/test contamination by requiring new extraction logic built while looking at candidate content). **The fix** (`item_align_v2.py`, new, does not modify `item_align.py`/`item_parser.py`/`corpus_probe.py`/`edition_align.py` — imports and reuses `match_guidelines`, `_norm`, `_sim`, `_SIM_FLOOR` unchanged): for bullet/sub-bullet-marker old items only, an identifier match is no longer accepted on its own — it must also clear `item_align._SIM_FLOOR` (0.75, the same already-frozen threshold used everywhere else in the method, not a new number chosen to flatter this test). If it fails, the id is treated as coincidental and the item falls through to the identical guideline-scoped T3/T4/T5 search the method already performs for non-id-matched items. Ordinal-marker items run through byte-identical, untouched code — no similarity check is added for them, since v1 and v2 are mathematically guaranteed identical there. The fix does NOT address T6's separate, undiagnosed guideline-mis-mapping mechanism (§54) — scoped to the T2 id-trust mechanism only. **Smoke-tested against already-closed, already-published Connecticut v2023.1→v2024.1 data** (not new test data — verification only): 168/1,241 bullet id-matches rejected, 131 (78%) rescued via the existing T3/T4 search, 37 (22%) remain unmatched, consistent with the separately-diagnosed T6 mechanism not being addressed by this fix. No Connecticut accuracy numbers against ground truth were computed here, to keep this a structural smoke test rather than a second test against already-used data. **Sample**: full census of all 32 bullet-marker old items (not a stratified subsample — population too small) plus a standard §5.1-style stratified draw of 60 ordinal-marker items (10 per tier, using the *original*, unmodified method's tier assignment as the stratification basis, ≈92 items total). **Annotators**: 2 (matching §5.3's original spec exactly — user's explicit choice, scoped down from the original round's 4 given the smaller sample), Cohen's κ, discussion-based adjudication on disagreements, `CANNOT_DETERMINE` excluded from scored comparisons as before. **Hypotheses, fixed now, before the sample is drawn or scored:** **H3′a** (primary) — on the bullet-item census, v2's correspondence accuracy exceeds v1's (original method's); confirmed iff the paired-difference point estimate is positive with a bootstrap 95% CI lower bound excluding zero, item-level only (n=1 pair, so no pair-level resampling is possible or meaningful here — stated as a scoping fact, not a deviation). **H3′b** — on the same bullet-item census, v2's accuracy exceeds B2's; identical confirmation criterion. **H3′c** — on the ordinal-item stratified sample, the method (v1=v2, identical there) exceeds B2's accuracy; identical criterion; an independent replication of the original H3's ordinal-item finding (§54.1: 76.64% vs 71.53%) on entirely fresh data. Benjamini-Hochberg applied across {H3′a, H3′b, H3′c} as its own family, separate from the original {H3, H4, H5} family already closed. | Continuing the user-directed decision to fold feasible future-work items into this paper before drafting it, following the same before-not-after discipline used for every prior test in this study. | Nothing about §52–§55's already-published results is edited or reopened. This entry commits the fresh pair, the fix, the sample design, and all three hypotheses' exact confirmation criteria before any of them is scored — the annotation round and the actual H3′ result follow in a subsequent entry once complete. |
| 2026-08-18 | **NEW LIMITATION DISCOVERED, retroactively affecting already-published results: `item_parser.py` has a guideline-boundary-detection failure mode that sweeps large stretches of topically unrelated content into a single guideline, present in BOTH the fresh H3′ pair and the already-published, already-scored Tennessee 2017→2018 confirmatory pair.** Discovered while investigating why both H3′ annotators independently answered `NONE` for 11/32 bullet-census items the original method scored `T1_id_exact` (its most trusted tier) — inspection showed the old item's guideline, "Delirium with HyperAgitation," actually contains 513 items in the fresh old edition (539 in the fresh new edition), against a corpus median of ~12-13 items/guideline, and reading a sample of that content directly shows it is not one coherent protocol: wound-care device instructions ("push the side buttons inward... locate wound edges"), vascular-access notes ("presence of indwelling port"), and splinting content ("rotating the foot to mid-line position") are all filed under a delirium/agitation heading. A systematic scan (median-relative outlier check, all four Tennessee editions) found the same pattern is NOT new to the fresh pair: the **already-published, already-scored** Tennessee 2017/2018 pair has the identical shape in different guidelines — "PEDIATRIC CARDIAC EMERGENCY Neonatal Resuscitation" (244/241 items) and "REFERENCE Pulse Oximetry" (215/214 items) — content-checked directly and confirmed equally incoherent (obstetric content — "membranes ruptured," "LMP if applicable" — and trauma content — "tourniquet application," "maintain systolic pressure of 90" — filed under a pediatric-cardiac/pulse-oximetry heading). Two further outliers per edition ("Patient Refusal or Declination of Care," 135 items in 2017/2018; "Vascular Access," 208/228, and "Pre-eclampsia and Eclampsia," 129/133, in the fresh pair) show the same size anomaly but were not individually content-verified — flagged as likely affected by the same mechanism, not confirmed. **Quantified impact on the already-published Tennessee-pair numbers** (§52-55, and everything pooled across all 4 pairs that includes Tennessee): of the original 60 sampled Tennessee items, 15 (25%) fall inside the two content-verified bloated guidelines; of those 15, **8 (53%) show the method's prediction disagreeing with the already-computed ground truth** — more than double the pair's overall ~33% error rate — and several of the disagreements are `CANNOT_DETERMINE`, consistent with annotators struggling to work inside an unreviewable 200+/500+-item "guideline." This is a real, previously-undiagnosed confound baked into results already reported as final. `item_parser.py` is frozen and this is not fixed here, per the same discipline that has governed every other limitation found in this study (§3.4's quarantine forbids modifying frozen behaviour in response to anything observed in test data, and this was found via test-data inspection). | Surfaced during H3′ annotation review, not sought; asked the user how to handle it once found, per the standing instruction that genuinely discretionary calls affecting already-reported results are not resolved unilaterally. | **No already-published number in §52-55 is edited, retracted, or recomputed here** — this entry appends a caveat, per the "append, never rewrite" discipline used throughout this table. Going forward: (1) the Tennessee-pair-specific and any pooled-across-4-pairs figures in §52-55 must be read with this confound disclosed — the true magnitude of its effect on those aggregate numbers has not been separately quantified (only the direct 8/15 sub-analysis above), and doing so would require rescoring with the affected guidelines flagged, not undertaken here since it would reopen an already-closed, already-reported result; (2) for the in-progress H3′ test, the user chose to exclude the 11 contaminated bullet-census items from scoring rather than let them dilute the T2-fix result, reported as a distinct, separate finding from the fix's own result, not folded into it — see the following entry for the H3′ result itself, scored on the remaining 21 clean bullet items plus the 60 ordinal items. |
| 2026-08-18 | **H3′ result: H3′a and H3′b UNTESTABLE (a real power problem, not a bug and not a disconfirmation); H3′c NOT CONFIRMED (a genuine tie).** Both H3′ annotators completed the 92-item workbook independently: Cohen's κ = 1.0000, 0/92 disagreements — verified before trusting it (the two files are not byte-identical, MD5s differ; the task asks annotators to copy exact item-ID strings, which naturally produces identical answers between two independently-correct raters; the original round's A/B pair also showed κ=1.0, §50, so this is a consistent pattern for this task, not a new anomaly). **H3′a/H3′b**: v2 vs v1 and v2 vs B2 on the 21 clean bullet items both came back exactly 100.00%/100.00%, CI [0.0, 0.0] — a result too clean to report without checking per §10. Checked: 20 of the 21 clean items are `T1_id_exact`, where v1 and v2 run byte-identical code by construction; only 1 item is T2-eligible (`S020`), and even there the fix's rejection check did not fire (`fix_overrode_id_match: False` — similarity already cleared 0.75). Checking the full pre-exclusion population clarifies this is not an artefact of excluding the contaminated 11: of all 32 original bullet-census items, only 2 (6.25%) were T2-tier to begin with, against Connecticut's 168/1,241 (13.5%) in the §55 smoke test — Tennessee's bullet markers are overwhelmingly stable between these two editions, unlike Connecticut's. **H3′a and H3′b are therefore UNTESTABLE with this fresh pair** — the population needed to test the fix does not exist in sufficient quantity here, which is different from either confirming or disconfirming it. **H3′c**: v1(=v2) vs B2 on the 60 ordinal items, exactly 71.67%/71.67% (43/60 each) — also checked before trusting (per §10, since an exact tie invited the same scrutiny §53.1's original H3 comparison got): itemwise, 38 both-correct, 12 both-wrong, and a genuine 5-vs-5 split (5 items only the method got right, 5 only B2 did) that happens to net to zero — a real substantive tie, not a trivial identical-prediction artefact. **H3′c is NOT CONFIRMED** (point estimate must be positive; here it is exactly 0.0), and does not clearly replicate the original H3 ordinal-item finding (§54.1: method ahead by 5.1 points, pooled across 4 pairs) — on this one fresh pair the two are evenly matched. Full account `FEASIBILITY.md` §57; numbers in `annotation_packets/h3prime_tennessee_2022_2024/h3prime_test_report.json`. Code state unchanged at `d3068ee` for the frozen pipeline; `run_h3prime_test.py` (new) is the only code added. | Fulfills the prior entry's "see the following entry" commitment; verification performed before this write-up per §10, applied to both the too-clean H3′a/H3′b result and the exact-tie H3′c result. | **The T2 identifier-trust mechanism (§54) remains diagnosed and mechanistically sound** — the fix's logic was validated by the §55 Connecticut smoke test (78% of rejected bullet id-matches correctly rescued) — but this follow-up could not confirm or disconfirm whether it improves real-world accuracy, because Tennessee (the only fresh pair available after Connecticut and Pennsylvania were confirmed exhausted) does not contain enough genuine T2/T6 bullet cases to test it. This is a genuine limit of the available fresh data, not a result to spin either direction. H3′c stands as a small, honest, less-favourable-than-original data point on a single additional pair, reported alongside rather than in place of the original four-pair finding. The §56 guideline-boundary-bug discovery — independently valuable, and found only because this follow-up's clean result was investigated rather than accepted at face value — is the more significant outcome of this whole follow-up study. With this entry, the H3′ follow-up is complete; the paper draft remains the only outstanding item. |
| 2026-08-18 | **CRITICAL CORRECTION, found during a full-study audit requested by the user before drafting the paper: the "four independent annotators" in the main round were two duplicated pairs, not four independent people, and the H3′ follow-up's "two annotators" (E, F) show the identical pattern.** Checked directly by file-content comparison (not just normalized-answer comparison, which is what every prior analysis in this study computed): `Annotator_A.xlsx` and `Annotator_B.xlsx` agree on **240/240** items; `Annotator_C.xlsx` and `Annotator_D.xlsx` agree on **240/240** items; the 43 items flagged for "4-rater disagreement" are exactly the 43 places the (A=B) and (C=D) sets disagree, and **all 43 are (2,2) splits — zero 3-1 splits occurred**, a pattern extremely unlikely from four genuinely independent raters (a 3-1 split requires only one of four opinions to differ; getting zero of them across 43 real disagreements is the signature of two duplicated pairs, not four independent judgments). Separately, `E.xlsx` and `F.xlsx` (the H3′ round, §11's prior entry) are not byte-identical (different MD5 hashes, ruling out literal file duplication) but agree on **92/92 rows across every field including free-text notes** — the same pattern, source unconfirmed (possibly two people who conferred, or one person filling both, or a coincidence too large to responsibly assume). **This means "Cohen's κ = 1.0000 (A/B, primary)" and "Fleiss' κ = 0.877 (4-rater, supplementary)" (§50, `FEASIBILITY.md`) are unsupportable as stated** — the first is a file agreeing with a duplicate of itself, the second is two opinions counted as four. The H3′ entry's own "the original round's A/B pair also showed κ=1.0, so this is a consistent pattern for this task" reasoning is undermined by this same correction — that consistency was between two duplication artefacts, not independent evidence the task produces high agreement. | Surfaced by the user's explicit request to check "everything from the beginning" of the study before drafting the paper, prompting a check this project had not previously performed: comparing raw file content between annotators directly, rather than only comparing normalized answers (which is blind to whether two files are actually the same underlying judgment). | **The salvage is real and the damage is contained.** §5.3 pre-registered exactly *two* annotators with Cohen's κ, and two genuinely distinct answer sets do exist in the main round (A/B-set vs C/D-set). Recomputed honestly: Cohen's κ = **0.8168** (240 items, 43 disagreements, 82.1% raw agreement) — well above §9's 0.60 abort threshold, and a materially more credible number than the erroneous 1.0000. **No ground truth or downstream metric changes**: every one of the 43 disagreements was already sent to real, discussion-based adjudication (§51) regardless of whether it was believed to come from 2 or 4 raters, so the adjudicated ground truth §6/H3/H4/H5 were computed against is identical either way — verified by re-running `run_full_comparison.py` after this correction and confirming `full_comparison_report.json` reproduces byte-for-byte. Fleiss' κ and every "4 annotators"/"960 judgments" claim are withdrawn from the study; `FEASIBILITY.md` §50 is corrected in place with a visible correction notice (not silently rewritten) per that document's own precedent for factual corrections (§44.4), since it is a narrative log rather than a frozen registration document — this entry is the corresponding append-only record here, per this document's stricter rule. **The H3′ round is a separate, still-open problem**: E/F's identical-including-notes pattern means H3′c and the §56 boundary-bug evidence currently rest on a single, unreplicated judgment, not a verified two-rater agreement — no κ can honestly be reported for H3′ at all. A genuinely independent second annotator for the same 92-item H3′ packet is needed before H3′'s reliability can be stated; this is tracked as an open follow-up, not resolved by this entry. |
| 2026-08-18 | **Sensitivity analysis: §56's guideline-boundary bug, applied consistently across all four pairs (not just Tennessee, which is all §56 originally checked), excludes 41/209 usable items (19.6%) and flips H3's point estimate from negative to positive.** `run_sensitivity_analysis.py` (new, one-off, no frozen file touched) applies §56's own mechanical outlier rule (guideline size >4x the edition's median, floor 50 items, excluding `<preamble>`) to each pair's old edition, then recomputes every §6 metric and H3/H4/H5 excluding items whose guideline is flagged. Pennsylvania has zero flagged guidelines; Connecticut's are dominated by its long bulleted appendices (overlapping, but mechanistically distinct from, §54's T2 marker-position finding). Before trusting the rule: spot-checked two Connecticut outliers directly — "NEW Central Line Access" shows the same contamination pattern as Tennessee (unrelated newborn-transport content in its tail), but "Abuse and Neglect of Children and the Elderly" reads topically coherent throughout despite being a size outlier — the rule is a known-imperfect, pre-specified proxy, used anyway because per-guideline manual judgement would itself be new undisclosed discretion; stated plainly, not hidden. **Result**: method accuracy raw 75.12%→78.57%, weighted 87.37%→92.35%; B2 accuracy raw 79.43%→76.79%; **H3 (method−B2) point estimate −0.0431→+0.0179 (raw), a sign flip** — still not confirmed (clean-subset CI [−0.036, 0.071] still crosses zero), but the direction reverses. T3 precision (H4) moves from 96.88% to a perfect 100% (n effectively 33, consistent with §56.4's direct finding that the bug-affected item was T3's only error). H5 also shifts favourably (+0.0035→+0.0570) without reaching significance. BH-adjusted on the clean subset: H4 p≈0, H3 p=0.4491, H5 p=0.5828. Full account `FEASIBILITY.md` §58; numbers in `annotation_packets/sensitivity_analysis_report.json`. | Requested by the user's full-study audit: §56 quantified the bug's effect on Tennessee's own 15/60 items but never on the pooled §52-55 figures the bug also sits inside of — a real gap between what was checked and what was reported, closed here. | **This is a sensitivity analysis, not a new confirmatory test — H3 remains formally NOT CONFIRMED under the pre-registered criterion either way**, and no hypothesis status changes. What changes is the honest interpretation of §53's headline: "B2 significantly beats the method" was itself partly an artefact of the parser bug, not purely a property of the structural method's design. The paper must report both the full-sample and clean-subset numbers side by side, not lead with the full-sample result alone now that a specific, quantified confound is known to move it. §54's bullet-marker mechanism is unaffected by this — it remains a real, separately diagnosed finding, not explained away by §56/§58. |
| 2026-08-18 | **Post-hoc baseline B5 (embedding similarity) design committed here, before any scored run — the audit's Phase 4, explicitly NOT a pre-registered hypothesis, added because §4.2's baseline table (B1-B4, all lexical/structural) has no modern embedding matcher and a 2026 reviewer will ask for one.** `baseline_b5.py` (new, does not modify any frozen file): identical scope and consumption rule to `baseline_b2.align_items_b2` — global search across the whole new-edition document, no guideline/section restriction, greedy one-to-one consumption in old-item document order — the only difference from B2 is the similarity function: cosine similarity between sentence embeddings (`app.rag.embeddings.get_embedding_provider`, the same provider the production RAG pipeline uses — `BAAI/bge-small-en-v1.5` via `sentence-transformers`, confirmed installed and working, not the TF-IDF fallback) instead of token Jaccard. **Similarity floor fixed at 0.85 before any scored run** — a documented convention for near-duplicate detection with sentence embeddings, explicitly NOT calibrated against this study's 209-item ground truth (no threshold search was run), matching §3.4's standing discipline against tuning parameters on test data even though B5 itself is post-hoc and this floor is not itself a pre-registered parameter. Smoke-tested against Tennessee before this entry: 98.0% matched (1,470/1,500), plausible and in the same range as B1-B4's smoke tests (92.5%-96.9%), no crash, no anomaly. **Test procedure**: score B5 against the same 209-item ground truth and CANNOT_DETERMINE exclusions already used for B1-B4/H3/H5, report descriptively — method vs. B5 and B2 vs. B5 accuracy comparisons, both raw and weighted, item-level bootstrap CI (matching the unit already used for every other descriptive baseline comparison in §55). **No hypothesis is attached to B5** — it was never part of §7's registered hypothesis family and none is added retroactively; this is exploratory context for the paper's discussion of whether the study's structural/lexical comparison holds up against current NLP practice, not a new confirmatory test. | Continuing the user-approved audit plan (`encapsulated-forging-corbato.md`) Phase 4: strengthen the paper against the most likely reviewer objection (no modern baseline) before drafting. | Nothing about §52-58's already-published results changes. This entry commits only the design and floor; the actual comparison has not yet been run as of this entry — see the following entry for the result. |
| 2026-08-18 | **B5 result: significantly ahead of the method on the full sample (the first statistically significant method-vs-baseline gap in either direction anywhere in this study); the significance does not survive the §56/§58 boundary-bug exclusion, run as its own sensitivity check.** Full sample (n=209): method 75.12%, B2 79.43%, B5 81.82% — the highest raw accuracy of any method tested. Method−B5 raw: −0.067, **95% CI [−0.129, −0.010], entirely negative** (weighted: −0.043, CI [−0.091, −0.001], also entirely negative). B2−B5: −0.024, CI [−0.057, 0.005], not significant. Before treating this as final: applied the same §58 sensitivity check, since B5 shares B2's unscoped global search — exactly the property that made B2 immune to the boundary bug's damage. On the clean subset (n=168): method accuracy improves the same way as §58 (78.57%), **B5's accuracy drops (81.82%→79.76%)** — the mirror image of B2's pattern, for the same reason (unscoped search disproportionately benefits from items where guideline-scoped search has a corrupted guideline to search within) — and method−B5 moves to −0.012, **CI [−0.077, 0.054], no longer significant**. Full account `FEASIBILITY.md` §59; numbers in `annotation_packets/b5_comparison_report.json`. Code state unchanged at `d3068ee` for the frozen pipeline; `baseline_b5.py` and `run_b5_comparison.py` are the only code added. | Fulfills the prior entry's "see the following entry" commitment; the sensitivity check was applied before write-up, following §58's precedent, once the full-sample result showed the same shape (baseline ahead of the method) that §58 already found was confound-driven for B2. | **No hypothesis status changes — B5 was never pre-registered and none is retroactively confirmed or disconfirmed.** What this adds: B2, B3, and B5 now show a **consistent pattern** — every baseline that searches the whole document unscoped has a full-sample advantage over the method that shrinks to non-significance once the boundary bug is excluded. This reframes the paper's central empirical finding: not "naive/modern baselines beat a hand-built heuristic," but "structural scoping is only as good as the structure-detection it depends on" — a generalizable claim the full-sample numbers alone would have obscured. |
| 2026-08-18 | **H4 quantified and reframed: close to tautological in this corpus, not the robust evidence it was previously weighted as.** T3 is defined as identical text within a correctly-mapped guideline, so its 97-100% precision (§52.2, §58.3) risks reading as near-guaranteed rather than demonstrated. Checked the two concrete ways a T3 assignment could still be wrong, across the full T3 population (68 items, all four pairs, not just the 34 sampled for annotation): boilerplate collision (≥2 identical-text candidates in the new-edition pool) — **0/68 (0.0%)**, never occurred; guideline mis-mapping exposure (the mapped guideline's title actually changed between editions) — **2/68 (2.9%)**, Pennsylvania only. Neither failure mode had meaningful exposure. Full account `FEASIBILITY.md` §60. | User's explicit choice, after being shown the quantification, to reframe rather than defend H4 as non-trivial (the originally-chosen defense) once the numbers came back too weak to support it, or drop it entirely. | **H4's registered status is unchanged** (still confirmed under §7's mechanical criterion) — this is not a retraction. What changes is the paper's framing: H4 moves from "robust evidence" to **a sanity check confirming the pipeline behaves as designed**, not a demonstration that cross-edition correspondence is hard to get right. The paper's substantive weight shifts to the planned structure-quality degradation experiment and the §58-59 comparative findings. |
| 2026-08-18 | **Structure-quality degradation experiment design committed here, before any run — the audit's core new contribution, turning §56/§58/§59's accidental confound into a deliberate, controlled experiment.** Claim to test: structure-aware alignment beats text-only alignment only above some structure-detection quality threshold, and is actively harmful below it — a claim not found anywhere in the literature reviewed (tree-edit-distance/X-Diff/DocuToads-style prior art evaluates alignment quality directly, never as a function of upstream structure-detection error rate). **Design** (`structure_ablation.py`, new, does not modify `item_align.py`/`item_parser.py`/`corpus_probe.py`/`edition_align.py`): (1) parse all four pairs' eight editions once via the unmodified `parse()`, cache; (2) stamp `item._orig_id = item.item_id` on every item in the clean cache, on both old and new editions, before any corruption — `Item` is a plain dataclass so this is a bare attribute add, and it is what lets predictions be scored against the *existing* 209-item ground truth even after corruption rewrites `item_id`; (3) for each corruption rate r ∈ {0, 0.05, 0.10, 0.20, 0.35, 0.50} × 5 seeds, independently for each of the old and new editions: pick a random r-fraction of adjacent-guideline boundaries (ordered by `char_start`, matching physical document order) to remove, merging each resulting run of guidelines under its first guideline's title and rewriting `item_id` via the identical construction `item_align.py` itself already uses (`_norm_title(guideline)/section/marker_path` + preserved `#N` suffix) — a direct synthetic model of §56's actual mechanism (adjacent content swept into the guideline last successfully anchored on); (4) monkeypatch `item_align.parse` to return the corrupted, cached editions for the trial's `(old_pdf, new_pdf)` path pair, then call the real, **completely unmodified** `align_items` — this runs the frozen algorithm exactly as-is on corrupted input, with no risk of the ablation harness diverging from the real method's logic; (5) score against the existing ground truth via `_orig_id`, restricted to the same 209 already-sampled items used throughout this study. **B2 and B5 are NOT re-run per trial**: both operate purely on `.text`, never `.guideline`, and corruption here only rewrites `.guideline`/`.item_id` — their predictions are therefore provably invariant to this corruption, and their already-computed accuracy (79.43% / 81.82%, §53.1/§59.1) is held fixed as reference lines rather than redundantly recomputed. **Validity checks, fixed before running:** at r=0 the method's accuracy must reproduce 75.12% exactly and the usable-item count must be 209 — any deviation means the harness is broken, not that the finding changed; B2/B5 accuracy must be bit-identical across every r (the invariance argument above, made falsifiable); the method's accuracy curve is expected to be monotonically non-increasing in r, and a violation triggers investigation before any write-up, per §10. **r=0 is labelled "as-parsed," not "pristine"** — the real corpus already carries an unknown, non-zero baseline corruption rate (§56/§58's clean-subset analysis brackets roughly where it sits); this is stated plainly rather than presenting r=0 as an idealized baseline the real corpus doesn't actually occupy. Exploratory and post-hoc relative to the original §1-§9 registration — no new hypothesis from §7's family is created or tested; this does not confirm, disconfirm, or reopen H3/H4/H5. | User-approved plan (`encapsulated-forging-corbato.md`, novelty-audit phase): turn the accidental §56/§58/§59 finding into the paper's deliberate central contribution, since the method itself has no defensible novelty claim against existing tree-edit-distance and legislative-diff literature. | Nothing about §52-60's already-published results changes. This entry commits only the design, corruption model, and validity checks; the actual sweep has not yet been run as of this entry — see the following entry for the result. |
| 2026-08-18 | **Structure-quality degradation experiment result: all three pre-committed validity checks passed, and the sweep reveals a real, monotonic threshold relationship with an observed sign crossing against B2.** r=0 reproduced the existing result exactly (accuracy 0.7512, n=209, bit-identical to §53.1); the method's accuracy fell monotonically and substantially across the sweep — 75.12% (r=0) → 72.73% (r=0.05) → 69.38% (r=0.10) → 66.22% (r=0.20) → 60.29% (r=0.35) → 54.74% (r=0.50), monotonic within the pre-registered 0.01 tolerance at every step, no §10 investigation triggered; B2/B5 accuracy held fixed as pre-committed. Reading this alongside §58/§59's real (non-synthetic) clean-subset comparison (method 78.57% vs. B2 76.79%, an exclusion of items in guidelines already known bug-affected, not a true parser fix) shows the gap against B2 **inverts sign** between the as-observed corpus (−4.31 points) and the achievable clean subset (+1.78 points), and the gap against B5 shrinks by over 5 points (−6.70 → −1.19) and loses significance (already established, §59.2). The synthetic-corruption direction and the real-exclusion direction are reported as complementary, explicitly not merged into one continuous axis, since exclusion-by-guideline is an imperfect proxy for a true fix, not its inverse operation. Full account `FEASIBILITY.md` §61; numbers in `annotation_packets/structure_ablation_report.json`. Code state unchanged at `d3068ee` for the frozen pipeline; `structure_ablation.py` is the only code added. | Fulfills the prior entry's "see the following entry" commitment; validity checks were evaluated and passed before any interpretation was written up, per the pre-committed stopping condition. | **No hypothesis in §7's family is confirmed, disconfirmed, or reopened.** This is the audit's central new, defensible contribution: a genuine, empirically-grounded (not assumed) demonstration that a structure-aware alignment method's advantage over text-only baselines is a monotonic function of structure-detection quality, with an actual observed crossover within a plausible real-world quality range — a claim absent from the reviewed prior art (tree-edit-distance and legislative-diff literature evaluates alignment quality directly, never as a function of upstream structure-detection error rate). This becomes the paper's primary results section; §52-60 (H3/H4/H5, the baseline comparisons, the annotation correction) become supporting and validating material. |
| 2026-08-18 | **H4 tautology check: the reviewer objection is CONFIRMED, not refuted. H4 is demoted from a headline finding to a validation/sanity check.** Per the novelty-audit plan's chosen approach ("defend H4 as non-trivial"), `run_h4_exposure.py` measured whether T3's two real failure modes — boilerplate collision (>1 identical-text candidate) and guideline mismapping (old guideline's title changed and had to be matched, not trivially identical) — were ever actually possible for the 36 sampled T3 items, against `item_align.py`'s own candidate pools. A real bug was caught and fixed before running: naively comparing `item_align`'s already-mutated post-mapping guideline title against itself would trivially always agree, hiding mismapping entirely — fixed by parsing the old edition independently, before `align_items` mutates it in place, and zipping by index (parse()-order is preserved 1:1 into `_all_results`). **Result: 0/36 boilerplate collisions; guideline mismapping possible in only 2/36 (5.6%), both Pennsylvania.** 94.4% of sampled T3 items sat in a guideline whose title had not changed at all — a trivial identity lookup, not a real matching decision. | Continuing the user-approved novelty-audit plan's H4 defence step; the chosen approach was to defend H4, but the measurement itself must be reported honestly regardless of which outcome was originally preferred. | **H4 is demoted, not defended — the data does not support defending it.** This does not reopen or change §52.2's already-reported 97.06%/94.08% precision numbers, only their interpretation: they remain accurate but are no longer claimed as evidence the method's matching logic was meaningfully tested. The paper's results section leads with §61's degradation/threshold finding and the §53-59 comparative evaluation; H4 moves to a methods-validation subsection alongside the r=0 reproduction check. |
| 2026-08-18 | **Boundary-annotation (Workstream A, novelty-audit plan) design committed here, before any workbook is returned or scored — calibrates §61's synthetic corruption-rate axis against a REAL, measured structure-detection quality metric.** Scope: Tennessee's four editions only (2017, 2018, 2022-23, Sept2024) — the one format where §56's boundary bug is directly diagnosed, all four sharing the identical known-anchor code path, giving four independent measurements. Pennsylvania is excluded (zero flagged outliers in §58 — inferred as a near-clean operating point, not directly measured, and reported as an inference, not a measurement, if used). Connecticut is excluded categorically: its boundaries derive from its own Table of Contents (`detect_ct_toc_anchors`), so using annotator-read boundaries there would validate the parser against the same source it already reads — circular by construction. **Task**: two annotators independently read each PDF and record an ordered list of protocol/guideline titles only (not the recurring sub-section headings within them — an explicit, named list of Tennessee's own known template slots is given as a negative example) — a "hand-built table of contents", the same method that originally diagnosed §56, requiring no page/line-offset mapping (`canonical_text` offsets do not map back to physical PDF pages, since `extract_text` joins all pages with no boundary markers preserved — checked directly before choosing this design over a page-based one). **Scoring** (`run_boundary_scoring.py`, does not modify `item_align.py`/`item_parser.py`): the annotator's title list is aligned against `item_parser.parse(pdf).guidelines` via `item_align.match_guidelines` — **completely unchanged**, the identical function already used and validated for cross-edition guideline correspondence throughout this study, reused here to align two title lists instead. Its greedy one-to-one consumption is exactly the right behaviour for measuring §56's mechanism: if N real protocols were merged into one oversized guideline, only one annotator title can match it and the other N−1 correctly register as recall failures. Recall = matched annotator titles / total; precision = matched parser guidelines / total parser guidelines; F1 = harmonic mean. **Mandatory independence check, fixed before running**: verify the two returned files are not byte-identical AND that not every edition's title list is identical between annotators — a hard failure, not a soft warning, mirroring the discipline that should have caught the main round's annotator duplication the first time (`PREREGISTRATION.md`'s 2026-08-18 CRITICAL CORRECTION entry) and did not. If either check fails, the files are treated as an unverified collection failure and re-collection is required before any score is computed or reported. | User-approved novelty-audit plan, Workstream A: calibrate §61's synthetic corruption-rate axis against measured structure-detection quality, since a synthetic axis alone cannot be mapped onto any other system or cited against published benchmarks (DocLayNet ~81% mAP, PubLayNet ~97%). | Nothing about §52-62's already-published results changes. This entry commits only the design, scope, and mandatory verification gate; workbooks have been sent for completion and no result exists yet — see a following entry once they return. |
| 2026-08-18 | **Second-domain replication (Workstream B/C, novelty-audit plan): HC1-HC3 committed here, before any US Code result is computed.** Corpus: US Code Title 18 (Crimes and Criminal Procedure), two OFFICIAL USLM XML release points from `uscode.house.gov` — PL 117-81 (2021) and PL 118-158 (2024) — a genuine version pair, both release points independently confirmed to have actually amended Title 18 (checked against the site's own release-point listing before selection, not assumed). Downloaded with the user's explicit permission (~1.91 MB and ~1.95 MB). Chosen as the domain-opposite of EMS protocol PDFs: OLRC-published, machine-readable, with stable OFFICIAL section identifiers and clean chapter/section XML nesting — structure detection is close to perfect by construction, versus `item_parser.py`'s heuristic anchor detection on the EMS side. **Corpus construction** (`uscode_corpus.py`, does not modify any frozen file): builds genuine `item_parser.Item`/`ParsedEdition` objects directly from the XML (chapter → guideline, section → item, `item_id` set to the section's own official USLM identifier, not a reconstructed one) so the real, unmodified `item_align.align_items` can run on this domain via the identical monkeypatch-`parse()` pattern already used and validated in `structure_ablation.py` — no parallel alignment logic, no risk of the two domains diverging because of different code paths. Smoke-tested: 1,387/1,396 sections (old/new), 141 chapters both editions (stable), 75 repealed both editions (stable — a good sign nothing is silently broken), 1,312/1,387 (94.6%) baseline persistence — a plausible, non-degenerate churn rate for a real 3-year legal revision, not overwhelming and not trivially 100%. **Ground truth**: identifier persistence across release points, EXCLUDING sections repealed-in-place (a persisting identifier whose new-edition heading/text matches `\brepealed\b` is scored as a true deletion, not a correspondence — a repealed placeholder's content is genuinely gone even though its number is kept for numbering continuity). **B1/B4 (identifier-based baselines) are excluded from the headline comparison** — an a priori structural fact about this domain, not something requiring an empirical run to demonstrate: since ground truth here is DEFINED by identifier persistence, scoring an identifier-lookup baseline against it would be comparing the method to a close paraphrase of its own ground-truth definition. **Corruption model reused unchanged** from `structure_ablation.py` (`corrupt_edition`), same rate sweep r ∈ {0, 0.05, 0.10, 0.20, 0.35, 0.50} × 5 seeds, same monkeypatch pattern. **Hypotheses, fixed now, before any rate is run:** **HC1** — the method's accuracy is monotonically non-increasing in r (tolerance 0.01), mirroring §61's validity check. **HC2** — a crossover against B2 exists within the tested range (i.e. the method's gap vs. B2 is positive at some r ≥ 0), the domain-specific test of §61's real-world-range claim — full cross-referencing against the published 81–97% structure-quality range awaits Workstream A's real EMS boundary-F1 calibration, not yet returned. **HC3** — a crossover being present in BOTH the EMS domain (already observed, §61) and this independent US Code domain is what constitutes replication; a single-domain crossover must be reported as such, not generalized. B1/B4 exclusion, the repealed-section ground-truth rule, and the chapter-as-guideline mapping choice are all fixed here, before results, so none can be adjusted after seeing whether they flatter the outcome. | User-approved novelty-audit plan, Workstreams B and C: test whether §61's threshold/crossover finding is EMS-specific or a general property of structure-aware alignment, in a domain with the opposite structure-quality profile. | Nothing about §52-62's already-published results changes. This entry commits the corpus, ground-truth rule, exclusions, and all three hypotheses' exact criteria before any of them is scored — the actual sweep and result follow in a subsequent entry once run. |
| 2026-08-18 | **Second-domain replication result: HC1, HC2, and HC3 all confirmed.** r=0 method accuracy 94.59%, essentially tied with B5 (94.59%) and marginally ahead of B2 (94.30%, +0.29 pts) — a sane result given the corpus's own 94.6% baseline identifier-persistence rate (the pre-commitment entry's smoke test), i.e. the method is achieving close to the theoretical ceiling at near-perfect structure, as expected. **HC1 confirmed**: accuracy fell monotonically non-increasing (0.01 tolerance) at every step of the sweep — 94.59% (r=0) → 90.35% (0.05) → 86.46% (0.10) → 77.14% (0.20) → 61.56% (0.35) → 48.44% (0.50). **HC2 confirmed**: the crossover exists in the tested range, sitting almost exactly at r=0 — even a mild 5% synthetic boundary-merge rate is enough to put the method 4+ points behind both baselines, widening to 45+ points at r=0.50. **HC3 confirmed**: the same qualitative shape (near-perfect structure → method matches/leads; degraded structure → method trails, monotonically and substantially) appears in both a heuristically-parsed PDF corpus (EMS, §61) and a machine-readable XML corpus (US Code) — the strongest evidence in the study that the finding is a general property of structure-aware alignment, not a parser- or genre-specific artefact. The crossover sitting at r=0 here (versus needing exclusion-based cleaning to reach it in the EMS domain, §61) is consistent with, not contradictory to, a single underlying quality-advantage relationship: US Code's real operating point sits at the top of the curve (near-perfect structure by construction); EMS's sits lower (real, uncorrected parsing errors). Full account `FEASIBILITY.md` §63; numbers in `annotation_packets/uscode_experiment_report.json`. Code state unchanged at `d3068ee` for the frozen pipeline; `uscode_corpus.py` and `run_uscode_experiment.py` are the only code added. | Fulfills the prior entry's "see a subsequent entry" commitment; all three pre-committed validity/hypothesis criteria were evaluated exactly as specified before any interpretation was written. | **This is the paper's strongest new evidence**: the threshold/crossover relationship first observed by accident in the EMS domain (§56/§58/§59) and then demonstrated by deliberate controlled experiment (§61) now replicates in an independent, structurally opposite domain. Full quantitative cross-referencing against the published real-world structure-quality range (DocLayNet/PubLayNet) still awaits Workstream A's real EMS boundary-F1 measurement — this entry establishes the crossover generalizes across domains, not yet exactly where the real EMS corpus sits on the shared curve. |
| 2026-08-18 | **Boundary-annotation result: real corpus structure-detection F1 ≈ 0.794, closely matching published DocLayNet (~0.81 mAP, diverse real documents), far from PubLayNet (~0.97, scientific only).** First submission failed the mandatory independence check (all 4 editions' title lists identical between files, despite different byte hashes) — investigated rather than assumed identical to the main round's root cause, since this task is far more objective than correspondence judgement; user confirmed directly it was a submission error (same file uploaded twice under different names), not a duplication problem. Corrected resubmission passed cleanly: 0/4 editions identical, title-list lengths genuinely differ between annotators (81-97 vs. 119-164 items). **A real measurement artefact was then found and corrected before trusting the F1 numbers**: raw recall differed sharply between annotators (0.75-0.78 vs. 0.46-0.53) despite similar precision (~0.90-0.97 both) — traced to `match_guidelines`'s containment-biased scoring letting a short exact title (e.g. "Hypothermia") lose its rightful match to a longer sibling containing it as a substring (e.g. "Induced Hypothermia Following ROSC"), both scoring 1.0 against a short parser title — the *same* already-documented weakness Appendix B item 3 flagged for cross-edition guideline matching, now surfacing in this new use, not a new bug. Quantified: ~20-27% of "missed" titles are this collision artefact (a real, findable match that lost the greedy race); ~73-80% have no plausible parser match under any consumption order — genuine recall failures. Corrected F1 (treating collision misses as matched) used for calibration; raw numbers left unedited in `boundary_scoring_report.json`. Pooled corrected F1 across both annotators and all four editions: **0.7940** (`run_calibration.py`'s internally-consistent sweep) / **0.8034** (the standalone per-point check) — the two differ by ~1 point because `corrupt_edition` derives guideline lists from items only, excluding zero-item guidelines the raw `.guidelines` attribute includes; both legitimate, stated plainly rather than reconciled. Sweep (F1 vs. synthetic corruption rate) monotonically decreasing exactly as §61's accuracy curve: 0.7940 (r=0) → 0.7826 (0.05) → 0.7623 (0.10) → 0.7334 (0.20) → 0.6693 (0.35) → 0.5967 (0.50). Full account `FEASIBILITY.md` §64; numbers in `annotation_packets/boundary_annotation/boundary_scoring_report.json`, `boundary_scoring_corrected.json`, `calibration_report.json`. Code state unchanged at `d3068ee`; `run_calibration.py` is the only code added. | Fulfills the prior entry's commitment; both the independence-check failure and the collision-artefact discovery were investigated and disclosed before any calibration number was trusted, per §10. | **This is the number the whole calibration workstream exists to produce.** Placed on §61's curve: r=0 (the real observed corpus) sits at measured F1≈0.79, where the method achieves 75.12% accuracy, trailing B2 (79.43%) and B5 (81.82%). The real corpus's structure-detection quality is not an artificially weak parser — it sits in the same regime as published state-of-the-art layout-analysis systems on diverse real-world documents (DocLayNet), not the near-perfect regime scientific-document benchmarks (PubLayNet) or machine-readable formats (US Code XML, §63) occupy. The paper's calibration claim is now precise: at real-world structure-detection quality, the crossover has not occurred; it requires higher structure quality than this corpus, or a comparable real system, currently achieves. With this entry, Workstream A is complete. |
| 2026-08-18 | **CRITICAL CORRECTION, found during a ground-up full-project audit requested by the user before drafting the paper: a silent join bug dropped 24 of 233 usable items (10.3%), non-randomly, inflating the method's reported accuracy across every analysis from §53 onward.** `item_align.align_items` mutates `item_id` (rewrites it into the new edition's guideline vocabulary); `annotation_packet.csv` stores that post-remap id; every baseline (`baseline_b2.py`, `baseline_b1_b3_b4.py`, `baseline_b5.py`) keys its own results by the raw, pre-remap id instead — a string-keyed lookup mismatch for any item whose guideline was renamed, silently `continue`d past since `run_h3_test.py`. Measured: 24/233 items dropped (Tennessee 1, Pennsylvania 2, Connecticut #1 **21/58, 36%**, Connecticut #2 0); the dropped items scored ~37.5% method accuracy vs. 75.1% retained — the hardest cases, dropped non-randomly. Fixed via parse-order **index** join (`sample_join.py`, new, does not modify any frozen file): `align_items` and every baseline iterate the identical `parse()`d `old_items` list in the identical order (confirmed by direct code reading of all four baseline files plus `item_align.py`, not assumed), so `_all_results[i]` is stable across all of them regardless of mutated ids. **`structure_ablation.py` (§61) had the same bug class via a third id space** (`_orig_id`, stamped pre-mutation) — its own r=0 validity check was silently checking against the already-wrong 209/75.12% figure, passing its own guard undetected; fixed identically. `build_h3prime_sample.py`'s B2 column had the analogous exposure (v2's did not — it applies the identical remap `align_items` does); fixed, and verified to produce byte-identical H3′ results — a genuine negative finding for that pair, not an unchecked assumption. **Confirmed unaffected by direct code reading**: `run_uscode_experiment.py` (§63, ground truth and `_orig_id` both derive directly from raw XML with no CSV round-trip) and `run_calibration.py`'s own F1 measurement (§64, entirely separate ground-truth mechanism) — though §64.3's cross-referenced accuracy figures, borrowed from §61's stale reference lines, are corrected below. **Cross-checked against an independent, never-buggy code path**: `compute_section6_metrics` (§52) always computed correctly (71.24% raw, n=233, no baseline cross-reference at all); after the fix, `build_records()` reproduces 71.24% exactly — two independent implementations agreeing to four decimal places. Corrected headline numbers: method accuracy 71.24% (was 75.12%), B2 75.97% (was 79.43%), B5 78.11% (was 81.82%); method vs. B4 is **newly significant** (was not); method vs. B3/B5 remain significant; H3/H5 remain not confirmed; §58's clean-subset sign-flip against B2 survives but shrinks to +0.52 points (was +1.78); H4 remains confirmed, strengthened to a perfect 100% on the clean subset; a new significant unfavourable finding surfaces (method's false-correspondence rate significantly exceeds B1's on the clean subset, CI [0.046, 0.193]) that the buggy join had been hiding. Full account `FEASIBILITY.md` §65; every affected report JSON regenerated. Code state unchanged at `d3068ee` for the frozen pipeline; `sample_join.py` is new. | Surfaced by the user's explicit request for a ground-up audit ("ensure no errors are being made") before drafting the paper — the same discipline that has now caught four real bugs this study (`_norm_answer`, annotator duplication, the §56 boundary bug, and this join bug), none of them found by a result looking suspiciously good on its own; this one specifically was found by systematically re-deriving n from first principles rather than trusting a previously-reported figure. | **Every headline qualitative claim already published survives**: H3 not confirmed, H4 confirmed (now stronger), H5 not confirmed, the §58/§59 sign-flip patterns present in the same direction, §63/§64's core findings unaffected. **What changes**: exact magnitudes move by 1-5 points, generally against the method; method vs. B4 becomes newly significant; the clean-subset recovery against B2 is real but roughly a third the previously reported size; a new unfavourable significant H5-on-clean-subset finding is surfaced, not hidden. Reported in full per §10's standing discipline — a bug that happens to flatter a result is exactly what that discipline exists to catch, and it did. |
| 2026-08-18 | **Guideline tie-break stability check (audit Phase 2) design committed here, before any run.** `match_guidelines`' containment-biased score produces multiple perfect (1.0) candidates for a substantial share of old guidelines in both Connecticut pairs — measured directly: 31/92 (34%) v2022.1→v2023.1, 33/93 (35%) v2023.1→v2024.1; Tennessee 0/69, Pennsylvania 2/51. Ties are broken by `pairs.sort(reverse=True)` — alphabetical descending string order, deterministic but arbitrary with respect to anything the method measures. **Check**: `tiebreak_sensitivity.py` (new, does not modify `item_align.py`), `match_guidelines_randomized` — an exact copy of the frozen function's scoring logic (same token-overlap, same `_TITLE_FLOOR=0.5`), differing only in that tied candidates are broken by a random per-pair draw instead of alphabetical order. Monkeypatches `item_align.match_guidelines` for each of 20 trials, re-runs the real unmodified `align_items` on all four already-sampled pairs, and rescores the SAME 233 already-sampled items (sample held fixed) against the SAME already-collected ground truth — only how the method's own prediction is computed varies. Identity across trials tracked by **parse-order index** (`item_parser.py`'s own deterministic output order, independent of `match_guidelines`), not by the post-remap item_id, since remapped ids are exactly what varies here by construction. **Reference point, fixed before running**: 0.7124 (the actually-reported, alphabetical-tie-break accuracy, §65). **Reported regardless of outcome**: if the 20-trial spread is small, this retires the concern with evidence; if it is not small, that is a first-order limitation to disclose, not a reason to discard the check or average it away silently. | User-approved audit plan Phase 2, continuing the ground-up sweep requested before drafting the paper. | Nothing about §52-65's already-published results changes. This entry commits only the design and reference point; the actual 20-trial sweep has not yet been run as of this entry — see the following entry for the result. |
| 2026-08-18 | **Tie-break stability result: a real, material ±2.5-point-scale spread, but H3's qualitative conclusion holds under every tested alternative.** 20 trials, randomized guideline-matching tie-break, sample and ground truth held fixed: mean accuracy 72.47%, range **70.39%-75.54%, spread 5.15 points**. The reported alphabetical-tie-break figure (71.24%) sits toward the low end but is not an outlier (6/20 trials at or below it). **Checked directly**: does any trial reach B2's 75.97%, which would mean H3's "method behind B2" finding is itself a tie-break artefact? No — the maximum observed trial (75.54%) still falls 0.43 points short. H3's qualitative conclusion holds under all 20 alternatives, though by a much smaller margin than the point estimate alone suggests. Full account `FEASIBILITY.md` §66; numbers in `annotation_packets/tiebreak_sensitivity_report.json`. Code state unchanged at `d3068ee`; `tiebreak_sensitivity.py` is the only code added. | Fulfills the prior entry's commitment; the critical cross-check (does any trial flip H3) was run and reported before any interpretation was written, per §10. | **Not a concern retired by the check — a real, previously-uncounted source of uncertainty, disclosed rather than resolved.** Not silently folded into the existing bootstrap CIs, which measure a different thing (sampling variability given a fixed method) from this (algorithmic variability from an arbitrary implementation choice within the method). No tie-break rule is changed or "improved" after seeing this result — that would be exactly the post-hoc tuning this study's discipline exists to prevent. The paper must state plainly that ~34-35% of Connecticut's guideline mappings are effectively arbitrary among equally-scoring candidates, and that this study's point estimates carry this additional, measured uncertainty on top of sampling noise, even though its qualitative conclusions are robust to it. |
| 2026-08-18 | **B5 model + similarity-floor sensitivity grid (audit Phase 3) design committed here, before any cell is computed.** `baseline_b5.py`'s model (`bge-small-en-v1.5`, smallest in its family) and similarity floor (0.85) were both uncalibrated choices — exactly what a reviewer would press on, since B5 is this study's answer to "did you compare against a modern baseline?". **Grid, fixed now**: models {`bge-small-en-v1.5`, `bge-base-en-v1.5`, `bge-large-en-v1.5`} × floors {0.75, 0.80, 0.85, 0.90, 0.95}, 15 cells, **the entire grid reported regardless of outcome** — no cell may be selected or highlighted as "the" result after seeing which one flatters the comparison. `baseline_b5.py` refactored (not frozen, safe) to accept `model_name`/`sim_floor` and to expose `greedy_match_from_embeddings` separately from embedding — the floor gates acceptance in an already-computed similarity matrix and does not require re-encoding, but genuinely changes the matching outcome (greedy consumption order means an earlier item's floor-gated acceptance changes later items' candidate pool), so every floor is scored by a real re-run of the greedy loop; only the (expensive) embedding step is cached per (pair, model). `b5_model_floor_sweep.py` (new). Joined by parse-order index (`sample_join.py`), scored against the same 233-item ground truth every corrected analysis in this study now uses. Smoke-tested: `bge-base-en-v1.5` loads correctly (768-dim, vs. `bge-small`'s 384). Reference points fixed before running: method 0.7124, B2 0.7597 (§65). | User-approved audit plan Phase 3, continuing the ground-up sweep requested before drafting the paper. | Nothing about §52-66's already-published results changes. This entry commits only the grid and reference points; the sweep has not yet been run as of this entry — see the following entry for the result. |
| 2026-08-18 | **B5 sensitivity grid result: robust across all 15 configurations, and the originally-reported cell was not a favourable outlier.** Full grid (accuracy %): bge-small {0.75:74.68, 0.80:77.68, 0.85:78.11, 0.90:77.68, 0.95:76.39}; bge-base {0.75:75.97, 0.80:78.11, 0.85:77.25, 0.90:77.68, 0.95:75.97}; bge-large {0.75:76.39, 0.80:78.97, 0.85:78.11, 0.90:77.25, 0.95:75.11}. Grid mean 77.02%, range 74.68%-78.97% (4.29 points). **Every one of the 15 cells exceeds the method's accuracy (71.24%)** — the weakest cell still clears it by 3.44 points; B5 beating the method on the full sample does not depend on the specific model or floor chosen. Against B2 (75.97%): 11/15 cells exceed it, 2 tie exactly, 2 fall slightly below. The originally-reported cell (bge-small, floor 0.85 = 78.11%) sits 0.86 points below the observed grid maximum (bge-large, floor 0.80 = 78.97%) — not an outlier that happened to maximize the reported result; a properly-tuned search would if anything have reported a very slightly *stronger* B5 result than what was published. Full account `FEASIBILITY.md` §67; numbers in `annotation_packets/b5_model_floor_sweep_report.json`. Code state unchanged at `d3068ee`; `baseline_b5.py` extended (not frozen — added optional `model_name`/`sim_floor` parameters and factored out `greedy_match_from_embeddings`, fully backward-compatible) and `b5_model_floor_sweep.py` is new. | Fulfills the prior entry's commitment; the full 15-cell grid was computed and reported before any single cell was selected or highlighted, per the pre-commitment's explicit rule against post-hoc cell selection. | **Closes the audit's remaining "was this cherry-picked" question with a clean negative.** The paper can report a genuine sensitivity grid rather than one uncalibrated number, and state that the full-sample finding (unscoped baselines beat the structural method) is robust to reasonable embedding-model and threshold choices, not contingent on one specific configuration. With this entry, all three phases of the user-approved audit plan are complete. |
| 2026-08-18 | **Audit round 3, Phase 1: four statistical gaps closed, all additive — no point estimate changes anywhere.** Requested by the user for a fresh full-project sweep paired with literature research (see the next entry for the literature side). (1) **CIs on the §61 structure-quality curve** — previously point estimates only. Now reports two deliberately separate sources of variation: across-seed range (sensitivity to which corruption instance was drawn) and item-level bootstrap CI per seed (sampling uncertainty within one instance) — at r=0 the item-level CI alone is ~11.6 points wide (65.24%-76.82%), not conflated with the across-seed range. r=0 still reproduces 71.24%/n=233 exactly; monotonicity still holds. (2) **CI on the §64 calibration F1 anchor** — the 8 underlying edition×annotator points span 0.7313-0.8687 (13.7 points), bootstrap 95% CI [0.7597, 0.8472]; n=8 flagged as small enough that the raw range and per-annotator means are the more honest picture than the CI alone. §64.4's calibration claim confirmed robust to the full range. (3) **Null-centered bootstrap p-values**, computed via the standard pivot construction (shift the bootstrap distribution to center at the null, not the observed estimate) and reported alongside the existing percentile p-values — divergence negligible everywhere (max 0.0032, H3/H4/H5). (4) **H3′ Benjamini-Hochberg applied** across {H3′a, H3′b, H3′c} — pre-registered in the H3′ pre-commitment entry, never run until now: p_adjusted = {1.0, 1.0, 1.0}, with H3′a/H3′b's p=1.0 the mechanical consequence of the already-established UNTESTABLE finding (degenerate zero-variance bootstrap), not new evidence. Full account `FEASIBILITY.md` §68; every affected report JSON regenerated. Code state unchanged at `d3068ee`; `structure_ablation.py`, `run_calibration.py`, `run_full_comparison.py`, `run_h3prime_test.py` extended (none frozen). | Continuing the user-approved audit-round-3 plan, Phase 1 — the cheap, no-new-dependency statistical fixes, done first. | **No hypothesis status changes; nothing here is a new finding, only closed uncertainty.** Every fix was verified additive before being trusted: every pre-existing point estimate reproduced exactly, re-run and checked, not assumed. The paper can now report real error bars on its centerpiece curve, an honest uncertainty range on its calibration anchor, a second, more rigorous p-value construction with no material divergence from the first, and a fully closed set of pre-registration commitments for the H3′ follow-up. |
| 2026-08-18 | **B6 (LLM retrieve-then-rerank baseline, audit round 3 Phase 2) design committed here, before any of the 233 sampled items is scored.** Post-hoc, not pre-registered — same status as B5. Motivated by fresh literature research (see the next entry): the Ontology Alignment Evaluation Initiative's methodological evolution (LogMap → BERTMap → Agent-OM/LLMs4OM) mirrors this study's B1→B5 ladder almost exactly, and by 2026 an LLM matcher is the expected top rung; B1–B5 had none. **Design** (`baseline_b6_llm.py` + `run_b6_comparison.py`, new, do not modify any frozen file, or `baseline_b5.py`): retrieve-then-rerank, the field-standard pattern for cost reasons (one documented study spent $290 on unconstrained pairwise LLM matching; a 10k-class ontology implies ~10⁸ comparisons) — B5's existing, unchanged configuration (`bge-small-en-v1.5`) retrieves the top-k=10 candidates for each sampled old item by cosine similarity across the whole new document; the LLM (`gemini-2.5-flash`, temperature 0.0, model id pinned) is shown the old item and the 10 candidates and asked to pick one or answer NONE. **Only the 233 already-sampled items are ever scored — never the full ~1,500–2,000-item document per pair** — which is what keeps this inside the free tier (1,500 requests/day, no card required) rather than requiring the expensive unconstrained approach the cost literature warns against. **Design asymmetry, disclosed before running, not after**: unlike B1–B5, which solve the full document alignment problem under a global one-to-one consumption constraint, B6 is asked about each sampled item independently, with no consumption tracking across items — this plausibly *advantages* B6 (it can never lose a candidate to an earlier item's greedy claim the way B1–B5 can) and must be reported as a limitation of the comparison, not smoothed over. **Reproducibility**: every raw LLM response cached to a per-pair JSON file, so the artifact reproduces even if the API or model changes later; three retry attempts with exponential backoff before falling back to NONE on a persistent failure, logged as a warning, not silently absorbed. **Credentials**: `GEMINI_API_KEY` set by the user directly in their own environment; never read back, echoed, or stored by any code added here beyond the one environment-variable lookup needed to authenticate. Smoke-tested on 3 real items (all 3 preamble items, all correctly matched to themselves) before this entry. | User-approved audit-round-3 plan, Phase 2, following literature research identifying the missing top-rung baseline. | Nothing about §52–68's already-published results changes. This entry commits the design, model, retrieval configuration, and the disclosed asymmetry before any of the 233 items is scored — see the following entry for the result. |
| 2026-08-18 | **B6 result: statistically indistinguishable from a naive "always pick the top-1 retrieval candidate" shortcut — the model committed in the prior entry (`gemini-2.5-flash`) could not be run to completion, and the eventual result (`gemini-3.5-flash-lite`) is a real, mechanistically-verified null finding, not a data-integrity artefact.** The prior entry's model hit the free tier's real daily cap (20 requests/day for this project — far below the 250–1,500/day found in general research), and **222 of 233 items (95.3%) silently fell back to a forced NONE after repeated 429 errors**, producing a contaminated 27.04% "accuracy" that was caught before being reported or committed (§10) and discarded, not corrected in place. `gemini-2.5-flash-lite` (tried next) turned out deprecated for new users (404, redirects to `gemini-3.5-flash-lite`), which is what was ultimately used, after two cheap 32-call quota-headroom tests (one failed at 0/32, one succeeded at 32/32) rather than committing to the full run blind. **Full 233-item run, zero failed calls.** Method 71.24%, B6 72.96%; diff −0.0172, CI [−0.0815, 0.0472], not significant — the weakest of every baseline nominally beating the method (B2 75.97%, B3 78.54%, B4 77.68%, B5 78.11%). **Checked before trusting this** (§10, applied to a merely-unimpressive result the same as a suspiciously-good one): 229/240 sampled calls (94.6%) returned exactly "1"; directly compared against a naive always-pick-candidate-#1 shortcut (no LLM call at all) using the identical cached retrieval — **identical accuracy, 72.96% vs. 72.96%**, LLM agreeing with top-1 retrieval rank on 96.1% of items. In this specific setup, the lite-tier model adds no measurable value beyond its own retriever's ranking — verified directly, not assumed. Scoring code fixed mid-audit to record `call_succeeded` explicitly per item and exclude (never silently count) any forced-fallback answer. Full account `FEASIBILITY.md` §69; numbers in `annotation_packets/b6_comparison_report.json`, raw responses cached per pair. Code state unchanged at `d3068ee`; `baseline_b6_llm.py` and `run_b6_comparison.py` extended with `call_succeeded` tracking and model-parameterization after the first attempt's failure. | Fulfills the prior entry's commitment; the contaminated first attempt was caught and discarded before being reported, per §10, and the eventual clean result was itself checked against a null-model comparison before being trusted, per the same rule applied to a surprising-but-unimpressive outcome. | **A real, disclosed limitation of a specific model choice, not a claim about LLM-based matching in general.** `gemini-3.5-flash-lite` was selected for free-tier quota reasons, not capability; this B6 result is a lower bound on what an LLM reranker could achieve in this domain, and the paper must state that plainly rather than generalizing it. Does not replicate Agent-OM's reported OAEI strength on non-trivial correspondences (§69.1) — if anything shows the opposite pattern here. No hypothesis status changes; B6 is descriptive, same status as B5. |
| 2026-08-18 | **Docling as a second real mid-range structure-quality anchor (audit round 3, Phase 3) design committed here, before any of the four Tennessee editions is scored.** §61's curve had only two real anchors (our parser, F1≈0.79; US Code XML, ≈1.0) with everything between synthetic. IBM's Docling (installed fresh this session, `docling` 2.121.0) can supply a genuine third point. **Design** (`docling_boundaries.py` + `run_docling_calibration.py`, new, do not modify `item_align.py`/`item_parser.py`/`run_boundary_scoring.py`/`run_calibration.py`): our parser's item extraction and the existing 233-item ground truth stay completely fixed — ONLY guideline-boundary detection is swapped to Docling's `section_header`-labelled output, scored the identical way §64 scored our own parser (`match_guidelines`, frozen, unchanged, against the same two annotators' Tennessee boundary ground truth, with the same collision-artifact-corrected F1 from `run_calibration.py`'s `corrected_f1`, reused unchanged rather than reimplemented). **A real pitfall found and handled before scoring, not after**: Docling's `section_header` label mixes multiple hierarchy levels — verified directly on the 2017 edition, 735 raw `section_header` items including a running page-header ("TENNESSEE EMERGENCY MEDICAL SERVICES PROTOCOL GUIDELINES") repeated dozens of times, table-of-contents entries ("Index", "Index - Continued"), top-level category headers, and per-protocol sub-headers, all under the same label. **Decided against hand-classifying hierarchy levels** (a new, untested heuristic this study could not independently validate) **in favour of reusing `match_guidelines`' own token-overlap scoring to let noise self-penalize through the precision term** — the same property `run_boundary_scoring.py` already relies on for our own parser's output. The one filtering step applied before any score is computed: deduplicate to distinct header strings (matching how our own parser's `.guidelines` is inherently deduplicated), since counting the same repeated string as dozens of separate "candidate guidelines" would be an unfair denominator inflation unrelated to real boundary-detection quality. **Reference point, fixed before running**: our own parser's mean corrected F1 was 0.8034 (§64.2). Smoke-tested and debugged before this entry: an initial label-matching bug (`'DocLayNet.SECTION_HEADER'` assumed, actual string `'section_header'`) produced a false "0 headers found" result, caught by checking the full label distribution (735 section headers, not 0) before concluding anything about Docling itself — a labelling bug in the check, not a Docling failure. | User-approved audit-round-3 plan, Phase 3, after confirming install feasibility (130GB free disk, clean `pip install docling`) and a real smoke test rather than assuming either success or failure. | Nothing about §52-69's already-published results changes. This entry commits the design, the reused (not reinvented) scoring methodology, and the reference point before any edition is scored — see the following entry for the result. |
| 2026-08-18 | **Docling result: mean corrected F1 0.4069 across 4 Tennessee editions x 2 annotators, far below our own parser's 0.8034 — but decomposed before reporting, and the low F1 is a precision problem, not a recall problem.** Full 4-edition run completed (276/406/412/467 distinct Docling section headers per edition). Per-edition/annotator corrected F1: TN2017 0.3990/0.4948, TN2018 0.3005/0.3778, TN2022-23 0.3622/0.5151, TNSept2024 0.3191/0.4866; mean 0.4069. This is a large enough gap from Docling's own published DocLayNet benchmark (~0.864 mAP) that it was investigated before being reported, per §10, the same rule applied to any surprising result regardless of direction. **Decomposed directly** (Tennessee 2017, annotator 1, not assumed to generalize without stating so): 276 Docling headers vs. 81 real annotator-listed protocol titles, 71 matched — **recall 87.65%** (Docling finds most real boundaries, arguably better than what would be needed to beat our own parser on this axis alone) — **precision 25.72%** (the low F1 is entirely a consequence of over-segmentation: Docling's `section_header` label mixes running headers, ToC entries, category headers, and per-protocol sub-headers together, exactly the pitfall flagged in the pre-commitment). The consistently large header-count-to-real-title ratio across all four editions (3.4x-4.8x) is circumstantial evidence the same mechanism explains all 8 measurements, but this was directly verified for only the one point above, stated as such rather than generalized without checking. Full account `FEASIBILITY.md` §70; numbers in `annotation_packets/boundary_annotation/docling_calibration_report.json`. Code state unchanged at `d3068ee`; `docling_boundaries.py` and `run_docling_calibration.py` are the only code added. | Fulfills the prior entry's commitment; the surprising low-F1 result was decomposed into precision/recall before being reported as a bare number, per §10 applied to an unfavourable-looking result rather than only a favourable one. | **Docling is not shown to be a worse boundary-finder than our own parser — it is shown to over-segment relative to this study's specific "one header per protocol" granularity**, a mechanistically distinct failure mode from our own parser's under-segmentation (§56). Reporting the bare F1 alone would have materially misled a reader; the precision/recall decomposition is reported alongside it. This does not become a third clean point on §61's curve the way US Code did (§63) — F1 alone is not a fair placement given the demonstrated precision/recall asymmetry, and forcing it onto the axis without a like-for-like granularity match would overstate what was measured. Reported instead as a distinct methodological finding: general-purpose document-layout benchmarks (DocLayNet, PubLayNet) are not interchangeable with a system's measured performance on a specific downstream boundary-granularity task. With this entry, all three phases of the audit round 3 plan are complete. |
| 2026-08-22 | **CORRECTION: the boundary-annotation entry above (2026-08-18, "Boundary-annotation result") cross-references stale pre-join-fix accuracy figures in its implication column — "the method achieves 75.12% accuracy, trailing B2 (79.43%) and B5 (81.82%)" — which the join-bug fix two entries later (2026-08-18, "CRITICAL CORRECTION") superseded in every other location but this one.** Found during audit round 4's ground-up sweep, checking every occurrence of pre-fix numbers across both governance documents rather than assuming the join-bug entry's own "corrected below" language reached every cross-reference (it reached `FEASIBILITY.md` §65.3, which explicitly flags and corrects §64.3's identical stale cross-reference, but no equivalent correction was ever logged here). The measured F1≈0.794/0.8034 anchor itself is unaffected — only the accuracy figures it was cross-referenced against were stale. **Corrected figures**: at the real corpus's measured structure-detection quality (F1≈0.79), the method achieves **71.24%**, trailing B2 (**75.97%**) and B5 (**78.11%**) — not 75.12%/79.43%/81.82%. The qualitative conclusion is unchanged: the real corpus sits in the regime where the crossover has not occurred. Not the only occurrence of pre-fix numbers surviving in prose — both governance documents contain many correct, deliberately-preserved historical entries (dated before the join-bug fix, describing what was believed true at the time, per this table's append-only discipline) alongside this one now-corrected forward-looking cross-reference; entries dated 2026-08-17 and earlier 2026-08-18 rows in this table are historical record and are not retroactively edited. | Surfaced by audit round 4's explicit check of every `75.12`/`79.43`/`81.82` occurrence across both documents, prompted by the user's request for another full sweep. | No result changes; this is a documentation-accuracy fix, not a new finding. Closes the one cross-reference `FEASIBILITY.md` §65.3 had already fixed on its own side but this document had not. |
| 2026-08-22 | **CORRECTION: two scripts (`run_h3_test.py`, `diagnose_t2_mechanism.py`) still carried the pre-2026-08-18 id-string join bug independently of the fix applied to seven other drivers via `sample_join.py`.** Found during audit round 4's code sweep: both scripts built a baseline lookup keyed by raw parse-order `old_item_id` and matched it against the packet CSV's post-remap id, with `run_h3_test.py:144`'s guard comment reading verbatim "should not happen - same parse() on the same file" — the exact assumption `sample_join.py`'s own docstring identifies as false. `diagnose_t2_mechanism.py` produced `FEASIBILITY.md` §54's bullet-vs-ordinal decomposition, the mechanistic explanation for why H3 (structure vs. text-only) failed on bullet-marker items — a headline diagnostic that had never been recomputed under the fix, and had no JSON output to even flag as stale. Both scripts converted to `sample_join.build_index_join`/`join_baseline`/`verify_sample_identity`, the identical pattern already used elsewhere. `run_h3_test.py`'s rerun reproduces `full_comparison_report.json`'s H3 figures exactly (method 71.24%, B2 75.97%, diff −0.0472 [−0.0987, 0.0043]) — an independent cross-check, not merely a code change taken on faith. Its output renamed `h3_test_report.SUPERSEDED.json` with an in-file note, since `full_comparison_report.json` already supersedes it; the stale pre-fix `h3_test_report.json` (n=209) removed. `diagnose_t2_mechanism.py` given a JSON output (`diagnose_t2_mechanism_report.json`) and a bootstrap 95% CI on each marker-kind subgroup's diff — closing the gap the 2026-08-18 marker-kind entry explicitly left open ("no bootstrap CI was computed for the subgroup split"). See the following entry for the recomputed §54 result. | Surfaced by audit round 4's code sweep, checking every driver against `sample_join.py`'s conversion list rather than assuming all seven originally-converted drivers were the complete set. | Confirms `full_comparison_report.json`'s pooled H3 numbers are correct (independent cross-check via a second, now-fixed code path). §54's bullet-vs-ordinal decomposition itself required recomputation — see the following entry. |
| 2026-08-22 | **§54 recompute result: the bullet-marker finding shrinks but survives (CI now excludes zero); the ordinal-marker finding does not survive — its previously-reported +5.1-point advantage is no longer distinguishable from zero.** Recomputed under the prior entry's join-bug fix. Bullet-kind items: n=72→**88** (16 recovered), method accuracy **62.50%**, B2 **80.68%**, diff −22.22%→**−18.18%**, bootstrap 95% CI **[−0.2841, −0.0909]** — excludes zero, so this remains a real, statistically supported finding, just smaller in magnitude than originally reported. Ordinal-kind items: n=137→**145** (8 recovered), method **76.55%**, B2 **73.10%**, diff +5.11%→**+3.45%**, bootstrap 95% CI **[−0.0207, 0.0897]** — **includes zero**; the previously-reported "ordinal items favor the method by 5.1 points" is not robust to the join fix and must not be reported as a confirmed subgroup finding going forward. Connecticut v2023.1→v2024.1 (the pair driving the pooled significant effect) recomputed: bullet n=40, diff −35.00% (method 65.00%, B2 100.00%); ordinal n=19, diff 0.00% (both 89.47%) — the bullet mechanism is entirely concentrated in this one pair's bulleted medication-reference appendix, unchanged in direction from the original finding. Full recomputed numbers in `annotation_packets/diagnose_t2_mechanism_report.json`, which also retains the pre-fix figures for direct comparison. Code state unchanged at `d3068ee`; only `diagnose_t2_mechanism.py` and `run_h3_test.py` (neither frozen) were modified. | Fulfills the prior entry's "see the following entry" commitment; the recompute was run and its CI checked before this write-up, per §10 applied regardless of which direction the numbers moved. | **The bullet-marker mechanism (§54.1's original explanation for T2's failure under bullet renumbering) survives as a real, CI-confirmed finding at a smaller magnitude (−18.2 points, not −22.2).** The ordinal-marker "advantage" does not survive and should be described in any future write-up as "not statistically distinguishable from zero under the corrected join," not as a positive finding. This does not change H3's own confirmed status (still NOT CONFIRMED, `full_comparison_report.json` unaffected) — it changes only the mechanistic sub-analysis explaining H3's pooled result. |
| 2026-08-22 | **B7 (local cross-encoder reranker baseline, audit round 4 Phase 3) design committed here, before any of the 233 sampled items is scored.** Post-hoc, not pre-registered — same status as B5/B6. Motivated by fresh literature research into B6's null result: Voyage AI's 2025 benchmark and independent findings report purpose-built cross-encoder rerankers beating LLM rerankers by up to 15% NDCG@10 while being 48-60x cheaper/faster, and that Gemini Flash-class models can *degrade* ranking quality below the underlying retriever's own order on modern benchmarks — directly relevant given B6's own finding (96.1% agreement with naive top-1 retrieval, identical accuracy). B7 tests whether a reranker purpose-built for this task class, rather than a general LLM, adds value B6 did not. **Design** (`baseline_b7_reranker.py` + `run_b7_comparison.py`, new, do not modify any frozen file, `baseline_b5.py`, or `baseline_b6_llm.py`): **identical retrieval to B6** — `baseline_b6_llm.get_topk_candidates` reused unchanged (top-k=10 by cosine similarity, `bge-small-en-v1.5`, unscoped across the whole new document) — so any accuracy difference between B6 and B7 isolates the reranking step itself, not a retrieval change. Reranker: `BAAI/bge-reranker-v2-m3` via `sentence_transformers.CrossEncoder`, run locally, no API key, no rate limit, no cost — smoke-tested (identical-text pair scored 0.99998, an unrelated pair 0.00015, confirming sigmoid-activated [0,1] scores behave as expected before any real item is scored). Each of the 10 candidates scored independently against the old item's text; **NONE-prediction floor swept over {0.0 (always pick argmax, no NONE possible), 0.3, 0.5, 0.7}**, all four reported as a full grid rather than a single calibrated choice — the same discipline `b5_model_floor_sweep.py` already established, since any single floor picked after seeing results would be undisclosed discretion. **Design asymmetry, disclosed before running, identical in kind to B6's**: no one-to-one consumption constraint across the 233 sampled items, each scored independently — plausibly advantages B7 the same way it plausibly advantaged B6, and is reported as a limitation of the comparison, not smoothed over. **Reported against the naive top-1-retrieval shortcut** (no reranking at all, B5's own top candidate) — the same comparison that made B6's null result interpretable, computed once and reused for both. Only the 233 already-sampled items are ever scored. Reproducibility: no API calls, fully deterministic given the pinned model id and floor grid — no response cache needed the way B6's non-deterministic remote calls required. | User-approved audit-round-4 plan, Phase 3, following literature research (Voyage AI, ZeroEntropy) identifying purpose-built rerankers as the field-standard alternative B6's null result had not yet been tested against. | Nothing about §52-71's already-published results changes. This entry commits the design, retrieval reuse, floor grid, and the disclosed asymmetry before any of the 233 items is scored — see the following entry for the result. |
| 2026-08-23 | **B7 result: the purpose-built reranker is WORSE than doing no reranking at all, at every floor tested, significant at floor=0.0 — investigated before reporting per §10, and traced to a real, coherent mechanism, not a bug.** Naive top-1-retrieval-only accuracy 72.96% (matches B6's finding exactly, as expected — same retrieval). B7 accuracy: floor=0.0 **69.53%** (diff vs naive **−0.0343, CI [−0.0644, −0.0086], entirely negative — significant**), floor=0.3 70.82% (diff −0.0215, CI [−0.0558, 0.0086]), floor=0.5 72.10% (diff −0.0086, CI [−0.0472, 0.0300]), floor=0.7 70.82% (diff −0.0215, CI [−0.0601, 0.0172]) — B7 never exceeds the naive shortcut at any floor, and against the method itself (71.24%) none of the four floors reach significance either direction. **Investigated before trusting this** (§10, applied to a surprising-and-unfavourable result the same as a surprising-and-favourable one): of 233 items, the reranker disagreed with naive top-1 on 22 (9.4%) — 2 where the reranker was right and naive was wrong, 10 where naive was right and the reranker was wrong, 10 where both were wrong. **Direct inspection of the 10 naive-right/reranker-wrong cases found a consistent mechanism**: the reranker assigns near-1.0 confidence to matches that are clinically WRONG but share surface-level structural/terminology similarity with the true match — e.g. "5 2 1 Bilevel Positive Airway Pressure - Adult" scored 0.9999 against "New Transcutaneous Pacing - Adult and Pediatric" (an unrelated intervention), "Tachycardia Adult" scored 1.0 against the same transcutaneous-pacing item, "Ventricular Tachycardia with a Pulse" confidently matched to "Torsades de Pointe" (a related but distinct arrhythmia protocol). This is not truncation (item texts are short, well under `max_length=512`) or a retrieval failure (naive top-1, using the identical candidate list, picked the correct item in every one of these 10 cases — the true match was always present and ranked first by retrieval; reranking demoted it). The pattern is consistent with `bge-reranker-v2-m3` — trained for general open-domain relevance, not this specific document genre — weighting this corpus's own already-documented templated structure (§3.1, "the structure is templated, which matters more than the numbering") and shared clinical-protocol boilerplate over the actual distinguishing semantic content, exactly the kind of domain-transfer failure the retrieve-then-rerank literature warns reranking can introduce when the reranker was not evaluated on template-heavy specialized documents. Full account `FEASIBILITY.md` §72; numbers in `annotation_packets/b7_comparison_report.json`. Code state unchanged at `d3068ee`; `baseline_b7_reranker.py` and `run_b7_comparison.py` are the only code added. | Fulfills the prior entry's commitment; the surprising unfavourable result (a purpose-built reranker underperforming no reranking at all) was investigated via direct case inspection before being reported as a bare number, per §10. | **A second, independently-obtained negative result on top of B6's**: neither a general LLM (B6) nor a purpose-built cross-encoder reranker (B7) improves on simply trusting the embedding retriever's own top-1 ranking for this task, and B7 actively HURTS accuracy at its most permissive floor. This strengthens rather than merely repeats B6's conclusion — B7's failure mode is different and independently diagnosed (confident domain-transfer errors on templated documents, not "the model just echoes the retriever"), so the two results are complementary evidence rather than one finding stated twice. Neither B6 nor B7 is pre-registered; no hypothesis status changes. |
| 2026-08-23 | **Discriminability curves (audit round 4, Phase 3b) design committed here, before any B2/B5 accuracy is measured against corrupted input.** `structure_ablation.py`'s own docstring states B2/B5 accuracy is "provably invariant" to its synthetic guideline-boundary corruption (the corruption only ever mutates `.guideline`/`.item_id`, never `.text`, and B2/B5 search `.text` only) and cites a function, `verify_b2_b5_invariance`, as having checked this rather than merely assumed it. **Audit finding**: `verify_b2_b5_invariance` exists but is never called anywhere in `main()` or the module — grep-confirmed, two occurrences total (the definition and the docstring's own citation of it) — and even if called, its body only samples 3 of B2's clean predictions and returns them; it never runs B2 against corrupted input or compares anything. The invariance claim underlying the decision to hold B2/B5 fixed as flat reference lines on §61's curve was **never actually verified**, only asserted. This also directly answers this round's Finding 6 gap (Euzenat et al.'s discriminability property for degradation benchmarks: does the method's curve decline faster than a control's, or do both decline in parallel) — but the mathematical argument for invariance, unlike H1/H2's untestable status, IS checkable without new data collection, so this entry commits to actually checking it rather than reporting a curve that already assumes its own conclusion. **Design** (`annotation_packets/run_discriminability_curves.py`, new, does not modify `item_align.py`, `item_parser.py`, `structure_ablation.py`, `baseline_b2.py`, or `baseline_b5.py`): reuses `structure_ablation.corrupt_edition` with the IDENTICAL seed scheme `run_trial` already uses (`rng_old = Random(seed*2)`, `rng_new = Random(seed*2+1)`) so B2/B5's curve is corrupted by the exact same random instance as the method's curve at each (rate, seed) — a true apples-to-apples discriminability comparison, not an independently-sampled one. B2 reimplemented inline using `item_align._sim`/`_SIM_FLOOR` imported unchanged (the identical black-box functions `baseline_b2.py` itself uses); B5 reuses `baseline_b5.greedy_match_from_embeddings` unchanged, called directly on corrupted items rather than through `align_items_b5`'s PDF-path interface. **Scoring uses `_orig_id`** (stamped pre-corruption by `structure_ablation.load_and_stamp`, reused), exactly mirroring how `run_trial` already scores the method — this is the one methodological subtlety a naive re-run would miss: corruption rewrites `.item_id` to reflect the new merged guideline title, so a predicted item's CORRUPTED id string would spuriously fail to match ground truth even when the SAME underlying item (by position/identity) was correctly selected; `_orig_id` recovers true identity independent of the corrupted label. Embeddings for B5 are computed ONCE on the clean (uncorrupted) item texts and reused across every rate/seed, since `.text` is never touched by corruption and item order is preserved by `corrupt_edition`'s deepcopy — a deliberate compute-saving choice, not a shortcut that changes what is measured. **Falsifiable prediction, stated before running**: if B2/B5 really are invariant, their accuracy must be bit-identical across every rate and seed tested (matching the ALREADY-established r=0 values 75.97%/78.11% exactly at every point) — any deviation is investigated before write-up, per §10, the same standard already applied to the method's own r=0 validity check. **Seed count reduced to 2 per rate (not the method's own 5), decided before running after timing a single B2 trial at 21.6s/pair × 4 pairs ≈ 90s — the full 6×5×4 grid would run ≈70 minutes; 2 seeds still tests the full rate range and, since B2/B5's matching decisions never read `.guideline`/`.item_id` at all, a specific corruption instance mattering at one seed but not another would itself be informative** (it would mean the invariance argument is wrong in some instance-dependent way, not merely under-sampled) rather than something more seeds would meaningfully strengthen. r=0 needs no seed variation (`corrupt_edition` returns immediately, unrandomized, when rate≤0) and is run once. | Continuing the user-approved audit-round-4 plan, Phase 3b, and closing a gap the code sweep found in `structure_ablation.py` itself (a citied verification function that does not verify anything). | Nothing about §52-72's already-published results changes regardless of outcome. If confirmed invariant, this converts an asserted mathematical argument into an empirically checked fact and gives §61's curve two additional, genuinely non-trivial reference curves rather than two flat lines taken on faith. If NOT invariant, that is itself a significant finding requiring investigation before any part of §61 is trusted — see the following entry for the result either way. |
| 2026-08-23 | **CRITICAL FINDING: B2/B5 are NOT invariant to `structure_ablation.py`'s synthetic corruption, falsifying the "provably invariant" claim §61's curve was built on — root cause found and quantified: `corrupt_edition`'s synthetic item-id reconstruction creates real identifier collisions between genuinely distinct items, and this almost certainly also confounds the METHOD's own already-published §61 curve, not only B2/B5.** r=0 passed the sanity check exactly (B2 75.97%, B5 78.11%, matching every other driver in this study bit-for-bit) — but across the full sweep, both baselines' accuracy declines substantially with corruption rate: B2 from 75.97% (r=0) down to 68.67%-70.39% (r=0.50); B5 from 78.11% down to 60.52%-61.37% — a larger decline than B2's, and comparable in magnitude to the method's own reported decline over the same range. **Investigated immediately, per §10, given this directly contradicts a claim already published and quoted throughout both governance documents**: `corrupt_edition` reconstructs each corrupted item's `item_id` as `f"{_norm_title(new_title)}/{it.section}/{it.marker_path}{suffix}"`, preserving only a PRE-EXISTING `#N` suffix from the item's original id — it does **not** run the uniqueness-preserving `seen_ids` counter logic `item_parser.py` itself uses specifically because, per that file's own history, "282 colliding ids on a previous run" made this necessary for the REAL parser. Direct measurement, all 4 pairs, seed=1: **0 collisions at r=0** (as expected), **35-143 collisions at r=0.20** (2.9%-8.4% of items), **133-363 collisions at r=0.50** (11.0%-23.7% of items) — when two originally-distinct items from different source guidelines happen to share the same `section`/`marker_path` (extremely common: independently-numbered bulleted sub-lists like "1.", "2." or "a.", "b." recur constantly across different guidelines) and their guidelines get merged into the same run by corruption, their reconstructed ids become literally identical strings. B2/B5's greedy matching loop tracks consumption via a `set()` keyed by `item_id` — a collision means two genuinely different items are indistinguishable to the consumption tracker, so consuming one can spuriously block the other, and which of two colliding items "wins" becomes an artifact of iteration order, not text similarity. **This is a synthetic-corruption-model defect, not a real property of guideline-boundary quality** — it does not correspond to anything that happens when a real parser genuinely merges guideline boundaries (a real merge does not duplicate marker paths across unrelated content). Full account `FEASIBILITY.md` §73; numbers in `annotation_packets/discriminability_curves_report.json`. Code state unchanged at `d3068ee`; `annotation_packets/run_discriminability_curves.py` (new) is the only addition — `structure_ablation.py` itself was NOT modified, so **its already-published §61/§68.1 curve is unchanged and this entry does not correct any number already reported there** — it is a NEWLY DISCOVERED, DISCLOSED, NOT-YET-QUANTIFIED limitation on how that curve should be interpreted, logged the moment it was found rather than after further investigation, per this table's standing discipline of disclosing an open problem immediately rather than holding the entry until it is fully resolved. | Directly falsifies a load-bearing claim (`structure_ablation.py`'s docstring, quoted in this round's own Phase 3b pre-commitment entry) the moment it was tested — the exact scenario audit round 4's Phase 3b was designed to catch, converting an unverified assertion into either a confirmed fact or a real finding; here it is the latter. | **This is the most consequential finding of audit round 4 and directly bears on §61's status as "the audit's central new, defensible contribution."** Because `item_align.align_items` (the METHOD) is run on the SAME corrupted editions via the SAME id-reconstruction scheme, and the method's own T1 (exact-id) and T2 (identifier-trust) tiers explicitly depend on `item_id` string identity, **the method's own already-published accuracy decline across §61's curve is very likely partly or substantially an artifact of this same id-collision defect, not purely a measurement of guideline-boundary-detection quality as claimed.** This does not mean §61's qualitative finding (a monotonic relationship exists) is false — a genuine boundary-quality effect may well coexist with this artifact — but the MAGNITUDE of the reported decline, and therefore the location of the reported crossover point, cannot currently be trusted as clean. **This must be stated as an open, unresolved limitation in any paper draft**, and re-quantifying §61 with a corrected, collision-free id-reconstruction scheme (adding the same `seen_ids`-style uniqueness suffix `item_parser.py` already uses) is flagged as necessary follow-up work, not undertaken in this entry to avoid silently expanding this round's scope without a separate pre-commitment for that specific re-run. |
| 2026-08-23 | **Fix + re-quantification design committed here, before `corrupt_edition` is modified or §61's curve is re-run.** Given the severity of the prior entry's finding, this is undertaken as the necessary follow-up flagged there, not deferred further. **Fix**: add a `seen_ids`-style uniqueness counter to `structure_ablation.corrupt_edition`, mirroring `item_parser.py`'s own pattern exactly (a per-edition `dict[str, int]` counting occurrences of each reconstructed base id, appending `#N` on any repeat beyond the first) — applied AFTER the existing guideline-merge logic, so a genuine collision (two distinct items landing on the same reconstructed id) is resolved by disambiguating the id string, never by dropping or altering which items exist or what they are matched against. This is the smallest possible fix: it does not change the corruption model's semantics (which guidelines merge, or when), only guarantees the resulting ids remain what item ids always are in this study — unique. **Validity checks, fixed before running**: (1) r=0 must still reproduce the method's accuracy at 0.7124/n=233 exactly (unchanged corruption at r=0 means the fix cannot touch this); (2) re-run `run_discriminability_curves.py`'s own collision-count check first — 0 collisions at every rate confirms the fix actually works before trusting anything downstream; (3) only then re-run the full `structure_ablation.py` sweep (6 rates × 5 seeds, the method's own original design, not the reduced grid used for the discriminability check) and report the corrected curve **alongside**, not instead of, the original §61/§68.1 curve — both curves published together so the magnitude of the artifact's contribution is directly visible, per §10's standing preference for showing rather than asserting a correction's size; (4) re-run `run_discriminability_curves.py` itself under the fix to directly test whether B2/B5 are NOW genuinely invariant (the original claim, only true once the confound is removed) — if not, that would be a second, independent finding requiring its own investigation before any part of this fix is trusted. | Directly fulfills the "necessary follow-up work" flagged as unresolved in the prior entry — undertaken immediately given the finding's severity to this study's central empirical claim, rather than left open indefinitely. | Nothing about any already-published number is retroactively edited — old and new §61 curves are reported side by side. This entry commits the fix design and validity checks before any of them is run — see the following entry for the result. |
| 2026-08-23 | **Fix result: all four validity checks passed. The corrected curve declines LESS steeply than the original — roughly a fifth of the originally-reported decline at r=0.50 was the id-collision artifact, not genuine boundary-quality effect — and B2/B5 are now PERFECTLY invariant, confirming the original claim was true once the confound is removed.** (1) r=0 reproduced 0.7124/n=233 exactly, as required. (2) Collision recheck: **0 collisions at every rate, all 4 pairs** (was 35-363 pre-fix) — the fix works. (3) Full 6×5 sweep (mean accuracy): r=0 **71.24%** (unchanged); r=0.05 **69.78%** (was 69.27%, +0.51pt); r=0.10 **67.81%** (was 66.35%, +1.46pt); r=0.20 **65.24%** (was 63.09%, +2.15pt); r=0.35 **61.80%** (was 57.85%, +3.95pt); r=0.50 **57.00%** (was 53.05%, +3.95pt). Monotonic non-increasing still holds (confirmed within the pre-registered 0.01 tolerance). The gap between corrected and original grows with corruption rate exactly as the collision-rate measurement predicted (collisions were 0% at r=0, rising to 11-24% at r=0.50) — **at r=0.50, the corrected curve's total decline from r=0 is 14.24 points versus the original's 18.19 points: 3.95 of those 18.19 points (21.7%) were collision artifact, not genuine information loss.** (4) `run_discriminability_curves.py` re-run under the fix: B2 and B5 are now **bit-identical across all 11 trials** — B2 exactly 0.7597, B5 exactly 0.7811, at every single rate and seed tested, zero deviation. This is a clean confirmation, not merely an absence of a detected problem: the ORIGINAL "provably invariant" claim was correct in substance — the corruption model genuinely cannot affect B2/B5's decisions once ids are unique — the prior non-invariant result was entirely attributable to the collision bug, with no remaining unexplained variance. Full account `FEASIBILITY.md` §74; numbers in the regenerated `annotation_packets/structure_ablation_report.json` and `annotation_packets/discriminability_curves_report.json` (both overwritten with the corrected/confirmed values — the PRE-fix numbers are preserved in this table's prior entries and in `FEASIBILITY.md` §61/§68.1/§73's prose, per append-only discipline, not in the JSON artifacts themselves, which reflect current code state like every other report in this study). Code changed: `structure_ablation.py`'s `corrupt_edition` (not frozen) gained the uniqueness-counter fix; `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Fulfills the prior entry's four pre-committed validity checks, evaluated in the pre-committed order, before any interpretation was written up. | **§61's qualitative finding survives intact and is now on firmer ground than before this investigation**: the monotonic threshold relationship is real, not an artifact, and the crossover's existence is unaffected. **What changes**: the reported MAGNITUDE of the method's decline was overstated by up to ~4 points at the highest corruption rates — the corrected curve should replace the original in any paper draft, with both reported for transparency about the correction's size, not silently. This also retroactively validates every "B2/B5 held fixed as reference lines" design decision made throughout this study's ablation work (§61, §68.1) — the shortcut was mathematically sound, its only flaw was in the corruption harness's id-construction, now fixed. The most important outcome is procedural: this closes the loop opened by finding `verify_b2_b5_invariance` was a non-functional stub — the claim it was meant to check is now genuinely, empirically confirmed, not merely asserted a second time. |
| 2026-08-23 | **VLM boundary anchor (audit round 4, Phase 3c) design committed here, before any of the four Tennessee editions is scored.** §61's curve has our own parser (F1≈0.79-0.80, §64) and US Code XML (≈1.0, §63) as real anchors; Docling supplied a third point but failed on label granularity (§70: high recall 87.65%, low precision 25.72%, over-segmenting relative to this study's "one header per protocol" definition). A VLM prompted for exactly the SAME granularity the human annotators used, rather than a generic "section_header" layout label, tests whether the granularity mismatch — not a genuine boundary-finding weakness — was Docling's actual problem. **Design** (`vlm_boundaries.py` + `run_vlm_calibration.py`, new, do not modify `item_align.py`/`item_parser.py`/`run_boundary_scoring.py`/`run_calibration.py`/`docling_boundaries.py`): model `gemini-3.5-flash-lite` (the only model with confirmed headroom this study has established — B6's `gemini-2.5-flash` hit a 20-req/day cap, `gemini-2.5-flash-lite` is deprecated for new users), temperature 0.0, called via the Gemini Files API (`client.files.upload`) so the model reads the ACTUAL PDF natively (native multi-page PDF understanding, not a page-image-rendering pipeline built for this study) rather than pre-extracted text — a genuinely different input modality from every other boundary-detection method tested so far (our parser: text + layout heuristics; Docling: layout model; VLM: native document understanding). **Prompt mirrors the human annotator instructions verbatim** (`build_boundary_workbooks.py`'s `INSTRUCTIONS`/`KNOWN_SUBSECTIONS` constants, reused, not paraphrased): "list every distinct protocol's name, in document order, as you reach it — like building a table of contents by hand," explicitly excluding the same sub-heading list annotators were told to exclude ("Treatment Pathway, Paramedic Stop, Signs and Symptoms, AEMT Stop Here, EMT Stop Here, Paramedic, Reference, Paramedic Only, Medical Emergency, Procedure, Indications, Contraindications, Notes") — this is the one design choice expected to matter most, since Docling's failure was specifically a granularity mismatch and this directly targets it. Only 4 calls needed (one per edition, no retrieval/sampling), scored against the SAME two annotators' ground truth used for §64/§70, via `run_calibration.corrected_f1` reused unchanged. **Precision and recall reported separately, never F1 alone** — the explicit lesson from §70.3 (Docling's F1 alone would have misrepresented a real precision/recall asymmetry) and from the known confound `run_calibration.corrected_f1`'s collision-correction is more generous to a longer candidate list than a shorter one (§70, disclosed there and unchanged here). Reproducibility: every raw response cached to JSON per edition, same pattern as B6. Fail-loud on any API failure — do not silently fall back and report a contaminated result, per the B6 rate-limit incident's corrective (§69.3). | User-approved audit-round-4 plan, Phase 3c, continuing from §70's finding that Docling's failure was granularity, not boundary-finding capability. | Nothing about §52-74's already-published results changes. This entry commits the model, the granularity-matched prompt (reused verbatim from the human instructions, not paraphrased), and the precision/recall-separate reporting discipline before any edition is scored — see the following entry for the result. |
| 2026-08-23 | **VLM boundary anchor result: mean corrected F1 0.8954 — HIGHER than our own parser's 0.8034 — with genuinely balanced precision (0.9328) and recall (0.8754), unlike Docling's severe asymmetry.** Per-edition (annotator 1 / annotator 2): TN2017 F1 0.8753/0.9005 (97 VLM titles); TN2018 0.9752/0.8346 (80 titles); TN2022-23 0.8816/0.8993 (115 titles); TNSept2024 0.9844/0.8124 (95 titles) — VLM title counts (80-115) are much closer to the annotators' own counts (81-97 for annotator 1, 119-164 for annotator 2) than Docling's 276-467, direct evidence the granularity-matching prompt worked as intended. **Investigated before trusting this, per §10, applying the same scrutiny to a surprising FAVOURABLE result as to §69's/§72's/§73's surprising unfavourable ones**: (1) annotator 2's recall is consistently lower than annotator 1's across every edition (0.57-0.77 vs 0.95-0.97) — this is NOT a new anomaly, it is the SAME already-documented annotator-style gap §64.2 found for our own parser (annotator 1 averaged corrected F1 0.864, annotator 2 averaged 0.743, attributed there to annotator 2's longer, more granular title lists), reappearing consistently here rather than contradicting it — reassuring, not suspicious. (2) Precision is high (0.79-0.99) and recall substantial (0.57-0.98) simultaneously — the specific combination Docling could never produce given its label-granularity mismatch — direct evidence the VLM's title list genuinely operates at the annotators' intended granularity, not merely a lucky score. (3) The 80-title TN2017 smoke-test sample was read by hand before the full run and consists entirely of recognizable, correctly-formed clinical protocol names ("Torsades de Pointe", "Anaphylactic Shock", "Cerebrovascular Accident (CVA)") — no garbage, no running-header repeats, no ToC artifacts, the three failure modes that inflated Docling's counts. Full account `FEASIBILITY.md` §75; numbers in `annotation_packets/boundary_annotation/vlm_calibration_report.json`, raw titles in `vlm_raw_titles.json`, raw responses cached per edition. Code state unchanged at `d3068ee`; `vlm_boundaries.py` and `run_vlm_calibration.py` are the only code added. | Fulfills the prior entry's commitment; the favourable, higher-than-our-own-parser result was checked against the already-established annotator-gap pattern and read by hand before being trusted, per §10 applied symmetrically regardless of direction. | **This DOES become a genuine, clean third point on §61's structure-quality curve, unlike Docling** — the balanced precision/recall (unlike Docling's severe asymmetry) means F1 alone is not misleading here the way it would have been for Docling. More significantly: **a general-purpose VLM, given a granularity-matched prompt reused verbatim from the human annotator instructions, outperforms this study's own purpose-built rule-based parser at guideline-boundary detection (F1 0.8954 vs 0.8034)** — a substantive, reportable finding about the relative cost-effectiveness of prompt-engineered VLM document understanding versus a hand-built heuristic parser for this specific task, independent of and complementary to B6/B7's findings about VLM/reranker performance on the DOWNSTREAM matching task (where general models added no value) — boundary detection and item matching are evidently different-difficulty problems for current general-purpose models. |
| 2026-08-23 | **Appendix B item 3 audit workbook generated: "A manual audit of all accepted guideline pairs is required before publication," registered at pre-registration, never done until now.** `build_appendix_b_audit.py` (new, reuses `item_align.match_guidelines`/`_norm`/`_TITLE_FLOOR` completely unchanged — recomputes the same candidate scores `match_guidelines` already computes internally but does not expose, not a new scoring method) audits all 294 accepted guideline pairs across the 4 confirmatory edition pairs, ranking by two disclosed failure modes: **CONTAINMENT** (accepted score = 1.0 with unequal token-set length — the exact mechanism behind the documented "Hypothermia" → "Induced Hypothermia Following ROSC" collision) and **TIE** (one or more other candidates scored ≥ the accepted one — the mechanism `tiebreak_sensitivity.py` (audit round 3) measured affecting 34-35% of Connecticut's old guidelines). Result: **294 total accepted pairs, 73 flagged (24.8%)** — 8 containment, 66 tied (Tennessee 68 pairs/1 flagged; Pennsylvania 48/5; Connecticut #1 85/34; Connecticut #2 93/33 — the tie rate matching `tiebreak_sensitivity.py`'s prior 34%/35% measurement almost exactly, a consistency check the new script passed without being designed to). Written to `annotation_packets/Appendix_B_guideline_pair_audit.xlsx`, ranked flagged-first so a human reviewer sees a focused 73-row list rather than all 294, with Verdict/Notes columns for the review itself. **The review is not yet performed — this entry generates the instrument, closing the "audit never started" gap, not the audit itself**, which requires human judgement this entry does not substitute for. | Closes a gap registered as required before publication and never begun, surfaced again in audit round 4's document sweep. | The workbook exists and is ranked for efficient review; the actual audit (marking each flagged row CORRECT/WRONG/UNSURE) remains outstanding and is the user's to complete. No result changes until that review is done — this entry does not alter any accuracy figure. |
| 2026-08-23 | **H3′ second-annotator gap: found `Annotator_G_ANNOTATION.xlsx` (present in the H3′ directory, no generating script referencing it, provenance unconfirmed) is a BLANK, UNCOMPLETED template — 0/92 correspondence cells filled — not real ground truth as its presence might suggest.** The 2026-08-18 CRITICAL CORRECTION left this as an explicitly open follow-up ("a genuinely independent second annotator... is needed... tracked as an open follow-up, not resolved"). A fresh blind workbook, `Annotator_H_ANNOTATION.xlsx`, generated reusing `build_annotator_workbooks.py`'s own instructions/column-layout/blind-design formatting unchanged (the same helpers `build_h3prime_workbooks.py` already used for the now-retired E/F pair). New `run_h3prime_second_annotator.py` mirrors `run_boundary_scoring.py`'s `verify_independence` exactly (byte-hash AND cell-level answer comparison, raising rather than silently proceeding on a suspicious match) — the check that should have caught the ORIGINAL E/F duplication and is now built in from the start rather than applied after collection. **Verified the guard correctly distinguishes "file missing" from "file exists but blank"** — the original naive `.exists()` check would have treated G's blank template as complete; a completion-count check (`n_filled`) was added and confirmed to correctly report "0/92 filled (G), 0/92 filled (H)" rather than crashing or falsely proceeding. Neither G nor H has been completed by a human as of this entry. | Directly closes the pre-registration commitment left open on 2026-08-18, generating the instrument and its validation script before any data exists to validate — before-not-after discipline applied to instrument-building, not just to analysis. | **No H3′ number changes — this entry generates tooling, not data.** Once both `Annotator_G_ANNOTATION.xlsx` and `Annotator_H_ANNOTATION.xlsx` are genuinely completed by two independent people, `run_h3prime_second_annotator.py` verifies independence and computes Cohen's kappa automatically; until then, §57's H3′ results continue to rest on the single unverified judgment already disclosed there, and no κ is reportable for H3′. This remains an open, disclosed limitation, now with the tooling in place to close it the moment real data exists. |
| 2026-08-23 | **Fifth and sixth confirmatory pairs committed here, before any sample is drawn: Tennessee Sept2024→09.11.2025 (a third Tennessee link) and Connecticut v2024.1→v2025.1 (a third Connecticut link).** Both candidates (FEASIBILITY.md §79) already cleared, in a prior exploratory entry, the same acceptance-bar and size-outlier screening every existing pair cleared: Tennessee 94.6% trivially alignable / 5.0% preamble (identical to its own already-confirmatory sibling edition to one decimal place) / the same 3 known §56-pattern outliers at comparable magnitude; Connecticut 88.6% trivially alignable / 0.0% preamble (identical to its sibling) / 6 outliers matching its sibling's own count and substantially overlapping which guidelines are affected. **Revision-magnitude classification, completed here for the first time on both candidates, per §3.3's method (the publisher's own front-matter language, not external inference)**: Tennessee's new edition carries the identical "(revision project completed July 2024)" phrase, word-for-word, already found in the existing Sept2024 sibling; Connecticut's two new editions carry the identical "living document... can be edited and updated at any time. However, they are formally reviewed, edited, and released every two years" boilerplate, word-for-word, already found in the existing v2024.1 sibling. **Both classify as MINOR** — consistent with, not merely similar to, every one of the four existing pairs, since the classifying language is literally identical across old and new editions in both cases. **Consecutive-edition status**: Connecticut confirmed directly from the publisher's own official version-history page (v2024.1→v2025.1→v2025.2, nothing skipped); Tennessee has no public official archive to check against — the same already-disclosed limitation the existing Tennessee chain already carries (§44.3: "rests on the thinnest evidence of the four"), not a new gap specific to this candidate. **Sampling design**: reuses `annotation.stratified_sample` and `annotation.write_annotation_packet` completely unchanged — the identical frozen-seed (20261017) per-tier stratified draw and CSV/JSON/README packet format used for all four existing pairs, with no new sampling logic invented for these two. Packets to be generated immediately following this entry, before any annotation occurs. **This brings the confirmatory test set to 6 pairs from 3 publishers** — meeting the "≥6 pairs" component of the target an earlier entry (2026-08-17) flagged as "not yet met," though not its "≥4 publishers" component, which remains open. | Directly acts on the two vetted candidates from FEASIBILITY.md §79, completing the two checks that entry left outstanding (revision-magnitude classification, done above) before the one step properly requiring a decision (this pre-commitment itself). | Nothing about §52-80's already-published results changes — pairs 1-4 and every hypothesis test computed from them stand exactly as reported. This entry commits the sampling design before any sample is drawn — see the following entry, if any, for the sample draw itself and any packet-generation notes. |
| 2026-08-23 | **Sample drawn and blind annotator workbooks generated for both new pairs, per the prior entry's committed design, unchanged.** Tennessee (population by tier: T1 1764, T2 58, T3 0, T4 0, T5 34, T6 69) drew 60 items (T1 30, T2 10, T5 10, T6 10 — no T3/T4 population exists in this pair to draw from, matching the pattern the existing Tennessee 2017→2018 pair already showed). Connecticut (population: T1 1037, T2 338, T3 26, T4 3, T5 59, T6 89) drew 60 items (T1 16, T2 11, T3 10, T4 3, T5 10, T6 10). Both packets (`annotation_packet.csv`, `annotation_context.json`, `README.md`) written to `annotation_packets/tennessee_sept2024_20250911/` and `annotation_packets/connecticut_v20241_v20251/` via `annotation.write_annotation_packet`, unchanged. **Two blind annotator workbooks generated** (`build_new_pairs_workbooks.py`, new, reuses `build_annotator_workbooks.py`'s instructions/column-layout/blind-design formatting completely unchanged — the same reuse pattern already established for the H3′ follow-up), scoped to just these two new pairs rather than merged into the original 4-pair combined workbooks Annotators A-D already completed, since retroactively altering an already-submitted instrument is against this study's standing discipline. Two annotators, the next available letters after the main round's A-D and the H3′ follow-up's E-H: **Annotator I and Annotator J**. Neither has been completed as of this entry — this generates the instrument, exactly the same state the Appendix B audit workbook and the H3′ G/H packet already sit in, not a completed result. Code state unchanged at `d3068ee`; `build_new_pairs_workbooks.py` is the only code added. | Fulfills the prior entry's committed design; the sample was drawn and packets generated immediately following the pre-commitment, with no design change between commitment and execution. | **The confirmatory test set now stands at 6 pairs from 3 publishers, with 360 total sampled items across all six once these two are annotated (240 already collected + 120 new)** — pending Annotator I and J's completion, which is the user's to arrange, not something this entry can do. No hypothesis result changes until that annotation is complete; H3/H4/H5 and every other already-reported figure stand exactly as reported on the original 4-pair, 240-item dataset until a formal re-run against the expanded 6-pair, 360-item dataset is separately committed and performed. |
| 2026-08-23 | **Annotator_G_ANNOTATION.xlsx and Annotator_H_ANNOTATION.xlsx (the §65-committed H3′ second-annotator pair) genuinely completed by two people; independence verified before use, per the tooling §65 committed in advance.** `run_h3prime_second_annotator.py`'s `verify_independence` confirms different file hashes and cell-level answers between G and H (not a repeat of the E/F duplication). **Cohen's kappa 0.9414, observed agreement 94.57% (87/92)** — comfortably above every threshold this study uses (§9's 0.60 abort threshold; higher than the main round's own pooled kappa of 0.8168). 5 disagreements (S026, S027, S043, S049, S074), all the same pattern: one annotator said NONE (confidently deleted), the other CANNOT_DETERMINE (genuinely unsure) — a real interpretive question, not noise. **Separately, and logged here because it was found only while wiring G/H into the scoring script: `run_h3prime_test.py` (the script that actually computes H3′a/H3′b/H3′c, §57) was still pointed at the retracted `Annotator_E_ANNOTATION.xlsx`/`Annotator_F_ANNOTATION.xlsx` pair, with a docstring literally claiming "kappa=1.0000, 0/92 disagreements" — the exact same file-agreeing-with-a-copy-of-itself artifact the 2026-08-18 CRITICAL CORRECTION already found and retracted for the main A/B round, but never propagated to this script.** This means every H3′a/H3′b/H3′c number published in `FEASIBILITY.md` §57 to date was computed against fabricated ground truth, not merely against a single unverified annotator as §57 already disclosed — a strictly worse situation than what was disclosed, caught by §10's standing rule while doing unrelated wiring work, not by a targeted audit. **Fix, committed here before re-running**: `run_h3prime_test.py` repointed at `Annotator_G_ANNOTATION.xlsx`/`Annotator_H_ANNOTATION.xlsx`; `build_ground_truth()` rewritten to no longer raise on any disagreement, instead returning `(ground_truth, pending_sids)` — the 5 genuine G/H disagreements are excluded from ground truth and reported explicitly as pending human adjudication (`build_h3prime_adjudication.py`, new, generates a 2-rater adjudication workbook reusing `build_adjudication_sheet.py`'s styling for the main round's 43-item adjudication, adapted to 2 raters) rather than silently resolved by majority-of-one or dropped without disclosure. Deleted `Annotator_E_ANNOTATION.xlsx`/`Annotator_F_ANNOTATION.xlsx` from the repo (retracted, superseded, no longer referenced by any script). Code changed: `run_h3prime_test.py` (not frozen), `build_h3prime_adjudication.py` (new); `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Directly closes the §65 follow-up the moment genuine data existed, and additionally corrects a previously-undetected dependency on the retracted E/F artifact, found while doing so. | **Every H3′a/H3′b/H3′c number in `FEASIBILITY.md` §57 is superseded, not merely re-verified** — see the following entry for the recomputed result under genuine G/H ground truth. §57's text is left in place per append-only discipline, with a forward pointer to `FEASIBILITY.md` §89. |
| 2026-08-23 | **Annotator_I_ANNOTATION.xlsx and Annotator_J_ANNOTATION.xlsx (the §67-committed pair for the fifth/sixth confirmatory pairs, Tennessee Sept2024→09.11.2025 and Connecticut v2024.1→v2025.1) genuinely completed; independence verified, ground truth built, section 6 metrics computed.** `run_new_pairs_metrics.py` (new, mirrors `run_final_metrics.py`'s structure for the original four pairs and `run_h3prime_test.py`'s pending-adjudication treatment for 2-rater data) confirms via `verify_independence` (byte-hash + per-pair cell-level answer comparison) that I and J are genuinely distinct: different hashes, different answers on both pairs. **Cohen's kappa: Tennessee 0.9486 (3/60 disagreements), Connecticut 0.9456 (3/60 disagreements), pooled 0.9478 (6/120)** — all comfortably above §9's 0.60 abort threshold and consistent with the main round's 0.8168 pooled kappa and H3′'s 0.9414. 6 total disagreements (3 per pair) excluded from ground truth and reported as pending adjudication, following the identical pattern established for H3′'s 5 G/H disagreements rather than resolved automatically. Ground truth for 114 of the 120 items assembled via `majority_vote` (which reduces correctly to "both agree" for 2 raters, per `annotation.majority_vote`'s existing `>n/2` rule, unchanged); `compute_section6_metrics` (unchanged, the same function `run_final_metrics.py` already uses) applied against each pair's `annotation_packet.csv` predictions. Code changed: `run_new_pairs_metrics.py` (new, not frozen); `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Directly executes the §67 commitment now that both annotators have completed the packet, mirroring already-established patterns (main round's section 6 metrics, H3′'s pending-adjudication treatment for 2 raters) rather than inventing new methodology. | See the following entry for the actual metric values and their comparison against the original four-pair pooled result. |
| 2026-08-23 | **H3prime_Adjudication_5_items_COMPLETED.xlsx returned: all 5 pending G/H disagreements resolved to NONE.** `run_h3prime_test.py`'s `ADJUDICATION_FILE` hook picked it up automatically (no code change) and the full 92-item ground truth is now resolved with zero pending items. **Full recompute: H3′a/H3′b unchanged (n=21, a=b=1.0000, diff=0, p=1.0, CONFIRMED: False — the same degenerate-population result, none of the 5 newly-resolved items fall in the clean-bullet subpopulation). H3′c now scored on the FULL 60 ordinal items (was 57): a=0.7167, b=0.7167 — identical — paired diff (a−b) point estimate 0.0, 95% CI [-0.1, 0.1], p=0.5641. CONFIRMED: False.** The 3 newly-resolved ordinal items (all adjudicated NONE) happened to make v1's and B2's predictions agree in aggregate exactly where they previously diverged — investigated before reporting, per §10: this is not a coding artifact (BH-adjustment, bootstrap seed, and scoring logic are byte-identical to the prior partial run; only `gt`'s contents changed, exactly as `build_ground_truth()`'s pending-file hook is designed to do) but a genuine consequence of 3 specific items' truth value being NONE, on which both v1 and B2's predictions happened to already agree (correctly or not) before this recompute — a coincidence of which items were pending, not a systematic effect. Full report `annotation_packets/h3prime_tennessee_2022_2024/h3prime_test_report.json` (overwritten, superseding the 87-item partial result two entries above). No code changed beyond running the already-committed script against the newly-provided file; `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Directly executes the follow-up flagged as outstanding two entries above — the pending-adjudication mechanism worked exactly as designed the first time real adjudication data was supplied. | **This is now the final, complete H3′ result — no items remain pending.** H3′a/H3′b's conclusion is unchanged. H3′c's point estimate moved from a nominal +0.0175 favoring v1 (not significant, n=57) to exactly 0.0 (not significant, n=60, the full population) — both readings support the same conclusion (H3′c does not independently replicate H3's original ordinal finding at the pre-registered significance bar on this smaller Tennessee sample), and the fuller number, not the partial one, is what should be quoted going forward. `FEASIBILITY.md` §89's text is left in place per append-only discipline (it was already committed and pushed); the final n=60 result is added as new subsection §89.5 rather than an edit. |
| 2026-08-23 | **Appendix B item 3's manual audit (the pre-registered "manual audit of all accepted guideline pairs is required before publication" gate, its workbook generated at the 2026-08-23 entry above but not yet reviewed) is now complete: 291/294 accepted guideline pairs CORRECT, 3 WRONG (1.0% error rate), all three in Connecticut 2022.1→2023.1.** Reviewer marked all 294 rows (not only the 73 flagged by CONTAINMENT/TIE), and caught one WRONG pair OUTSIDE the flagged set - "Intraosseous Access" incorrectly accepted-paired to "NEW Central Line Access" (score 0.5, no CONTAINMENT/TIE flag triggered) - direct evidence the automated flagging heuristics, while catching 2 of the 3 real errors (both CONTAINMENT+generic-title cases, "Adult" incorrectly paired both directions with "Poisoning/Overdose/Substance Use Disorder – Adult"), are not exhaustive, and that reviewing the full 294 rather than only the flagged 73 was the right design (the workbook already listed all 294, flagged-first, per the original entry). No UNSURE verdicts, no blank verdicts - every row was resolved by the reviewer. | Closes the pre-registration item-3 publication gate, registered at pre-registration and left open until now (the workbook-generation entry above explicitly stated "the review is not yet performed"). | **A 1.0% (3/294) wrong-guideline-pair rate is disclosed, not silently accepted.** All three wrong pairs are confined to one pair (Connecticut 2022.1→2023.1) and one failure family (generic single-word titles - "Adult" - colliding with specific protocols, plus one genuine distinct-protocol false match). This does not trigger a recompute of that pair's already-published section 6 metrics: 3 wrong guideline-level pairs out of hundreds of items is within the noise already reflected in that pair's own accuracy figures, and no other pair shows any wrong verdict at all. Recorded here as a disclosed, bounded limitation for any paper draft citing Connecticut 2022.1→2023.1's guideline-pair quality specifically, not as grounds to revise any headline number. |
| 2026-08-23 | **Adjudication instrument generated for the 6 pending Annotator I/J disagreements (3 Tennessee, 3 Connecticut) flagged when I/J's independence and ground truth were first assembled, two entries above.** `build_new_pairs_adjudication.py` (new, reuses `build_h3prime_adjudication.py`'s 2-rater layout and `build_annotator_workbooks.py`'s `load_pair`/`render_new_guideline` unchanged, adding a `pair` column so a single sheet spans both new pairs, matching `Adjudication_43_items.xlsx`'s pair-in-column-1 convention) wrote `NewPairs_Adjudication_6_items.xlsx`. `run_new_pairs_metrics.py`'s `ADJUDICATION_FILE` path updated to `NewPairs_Adjudication_6_items_COMPLETED.xlsx` to match. Not yet reviewed by a human. Code changed: `build_new_pairs_adjudication.py` (new), `run_new_pairs_metrics.py` (filename constant only); `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Generates the instrument for the follow-up flagged in the prior entry, before any adjudication data exists, mirroring the H3′ instrument-then-data-then-recompute sequence already used successfully in this study. | Nothing about the interim 110-item pooled result changes here - once a human completes the workbook, re-running `run_new_pairs_metrics.py` will pick it up automatically via the same optional-file hook already proven to work for H3′. |
| 2026-08-23 | **Fifth/sixth pair section 6 metrics: pooled weighted correspondence accuracy 86.67% (n=110 usable of 114 resolved), consistent with the original four-pair pooled 85.26% — but T5 (moved-guideline) precision is 0% on the new pairs (n=20) versus 13.3%/6.15% originally (n=30), a genuine, disclosed weakness, not smoothed into "consistent."** Tennessee (n=57 resolved, 3 pending): correspondence accuracy raw 64.91%/weighted 88.91%; deletion recall 60%/14.97% weighted; T1 precision 93.1%, T2 87.5%, T5 0% (n=10), T6 30%. Connecticut (n=57 resolved of 60, 3 pending, 4 cannot_determine): correspondence accuracy raw 64.15%/weighted 83.67%; T1 100%, T2 33.3% (n=6, small-n), T3 100%, T4 100%, T5 0% (n=10), T6 44.4%. Pooled: cannot_determine_rate 3.51%, correspondence accuracy raw 64.55%/weighted 86.67%, provenance_loss_rate weighted 3.12% (close to the original 1.48%), false_correspondence_rate weighted 10.97% (close to the original 13.93%). **T5 dropping to 0% precision (20/20 wrong) on fresh data, versus a already-weak-but-nonzero 13.3%/6.15% on the original four pairs, is the single most notable new finding here** — investigated before reporting, per §10: both new pairs' T5 items are genuinely "moved" cases (guideline reassignment) where the method's predicted item_id differs from the adjudicated truth on every single sampled instance across two independent publishers, not a single-pair fluke or a sampling artifact (n=10 each, drawn independently under the same frozen seed/tier design as every other pair). This sharpens, not creates, an already-known weak spot (T5 was already the method's worst-performing tier in the original four pairs) into a clearly worse one on independent replication. Full report `annotation_packets/new_pairs_final_metrics.json`. | Reports the recomputed result exactly as produced, including the unfavourable T5 finding, without selective emphasis. | **The core replication succeeds**: overall weighted accuracy, provenance loss, and false-correspondence rate all land within a few points of the original four-pair pooled figures on two entirely independent publishers and editions, supporting this study's central generalization claim. **T5 (moved guidelines) is now confirmed, on independent data, as the method's most unreliable tier** — any paper draft using tier-level precision figures must report T5's 0% alongside the pooled headline number, not omit it. Once the 6 pending-adjudication items resolve (`run_new_pairs_metrics.py`'s `ADJUDICATION_FILE` hook, awaiting a human-completed workbook analogous to H3′'s) and once a formal combined 6-pair/360-item re-run of H3/H4/H5 is separately committed and performed (flagged as outstanding in the §67 entry, still true here), this section's numbers should be read as a preliminary two-pair replication, not yet folded into the study's headline pooled figures. |
| 2026-08-23 | **H3′a/H3′b/H3′c recomputed against genuine G/H ground truth (87/92 items; 5 pending human adjudication, tracked separately, not scored either direction).** Of the 92-item packet: 11 are the already-disclosed §56-pattern contamination (bullet-census items landing in "Delirium with HyperAgitation," excluded from scoring exactly as the retracted E/F run also excluded them — this exclusion is unchanged by the G/H fix), leaving 21 clean bullet items and 60 ordinal items (57 of the 60 have resolved ground truth; 3 fall among the 5 pending). **H3′a (v2-fixed vs v1-original, clean bullet, n=21): a=1.0000, b=1.0000, diff=0, CI [0,0], p=1.0 — CONFIRMED: False.** **H3′b (v2-fixed vs B2, same 21 items): identical, a=1.0000, b=1.0000, diff=0, p=1.0 — CONFIRMED: False.** Both reproduce the already-published §57 finding that this subpopulation is degenerate by construction (20/21 items are T1-tier, where the fix is a no-op) — the p=1.0 is the expected UNTESTABLE result, not evidence against, consistent with §57's own prior characterization. **H3′c (v1 vs B2, ordinal, n=57 of 60 — independent replication of the original H3's ordinal finding, §54.1, on fresh Tennessee data): a=0.7544, b=0.7368, paired diff (a−b) point estimate 0.0175, 95% CI [-0.0877, 0.1228], p=0.441 — CONFIRMED: False.** Benjamini-Hochberg applied across the {H3′a, H3′b, H3′c} family as pre-registered (§65's predecessor commitment) for the first time — all three p-values remain 1.0/1.0/1.0 after adjustment. Full report `annotation_packets/h3prime_tennessee_2022_2024/h3prime_test_report.json`; second-annotator verification `h3prime_second_annotator_report.json`. Code state unchanged at `d3068ee` for the frozen pipeline; `run_h3prime_test.py` is the only scoring code, per the prior entry. | Directly executes the recompute the prior entry's fix made possible, on the first attempt, no further code change needed. | **H3′c's direction matches the original H3 ordinal finding's direction (fixed-population accuracy nominally higher than B2's baseline-only accuracy) but is NOT independently statistically significant on this smaller, fresh 57-item sample — the CI crosses zero.** This is reported as a non-replication at the pre-registered significance bar, not smoothed into "consistent with." It does not retract H3's original, separately-powered finding (§54.1, its own dataset), and 5 items remain genuinely undecided pending adjudication (`H3prime_Adjudication_5_items.xlsx`, generated, awaiting a human reviewer) — a second recompute once those resolve could shift n from 57/21/21 to 60/21/21 for the ordinal arm, though 3 additional items are unlikely to flip a CI this wide. Full account `FEASIBILITY.md` §89. |

---

| 2026-08-24 | **Formal H3/H4/H5 re-run against the expanded 6-pair, 360-item dataset, committed here before running: extends `run_full_comparison.py`'s exact method (identical stat functions, identical bootstrap/BH code, identical index-join fix) to the two new pairs (Tennessee Sept2024→09.11.2025, Connecticut v2024.1→v2025.1), using I/J's ground truth in place of A/B/C/D's.** `run_full_comparison_6pairs.py` (new) factors `run_full_comparison.py`'s `build_records`/hypothesis-test body into reusable functions imported, not reimplemented, and calls them three ways: on the original 4 pairs alone (a sanity-check recompute, expected to reproduce `full_comparison_report.json` exactly), on the new 2 pairs alone, and pooled across all 6. **PRELIMINARY, disclosed as such**: 6 of 120 new-pair items (3 per pair) remain pending human adjudication (`NewPairs_Adjudication_6_items.xlsx`, sent to the user, not yet returned) and are excluded from every number here, not estimated — following the exact same pending-item treatment already established for H3′ and for the new pairs' own §6 metrics. Code changed: `run_full_comparison_6pairs.py` (new, not frozen); `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Directly fulfills the commitment repeated in every entry touching the fifth/sixth pairs since §67: "H3/H4/H5... stand exactly as reported on the original 4-pair... dataset until a formal re-run against the expanded 6-pair... dataset is separately committed and performed." | Nothing about the original 4-pair result is altered by this entry — see the following entry for the recomputed values and the sanity check confirming the original-4-pair recompute reproduces `full_comparison_report.json` exactly (a required precondition for trusting the pooled number). |
| 2026-08-24 | **Result: the original-4-pair sanity check reproduces `full_comparison_report.json` exactly (H3 item-raw point −0.0472, H5 item-raw point 0.0501, both to 4 decimal places, n=233) — confirming the extended script's method is faithful before trusting its pooled output. Pooled 6-pair (n=343 of 353 usable, 6 items still pending adjudication elsewhere in the packet plus the 4 H3-population `cannot_determine` exclusions already priced into the original count): H3 NOT CONFIRMED (item-raw point −0.0204, 95% CI [−0.0641, 0.0233] — crosses zero, and less negative than the original 4-pair's −0.0472, though still not favoring the method), H4 CONFIRMED (T3 precision 97.67%, well above the 0.80 bar, BH-adjusted p≈0), H5 NOT CONFIRMED (method−B1 false-correspondence rate item-raw point +0.0568, 95% CI [0.0079, 0.1045] — entirely above zero this time, unlike the original 4-pair's CI which straddled zero, though the confirmation bar requires the CI's UPPER bound below +0.05, which it still exceeds).** New-2-pairs-only breakout (n=110): H3 point +0.0364 (method now nominally AHEAD of B2, reversing the original 4-pair's direction) but CI [−0.0364, 0.1091] still crosses zero at item level (pair-level CI [0.0, 0.0702] with only 2 pair-units, too few for a meaningful pair-level bootstrap, reported for consistency only); H4 100% (10/10 T3 items, small-n); H5 point +0.0775, CI [0.011, 0.1508] — worse than the original pairs' H5 point on this small sample. Investigated before reporting, per §10: the pooled H3 point moving from −0.0472 toward 0 (not confirmed either way, but a real shift) is driven by the new pairs' own reversed sign, not a computation artifact — confirmed by the new-pairs-only breakout showing the same reversal in isolation, and by the exact-reproduction sanity check ruling out a code-level explanation. Full report `annotation_packets/full_comparison_6pairs_report.json`. | Directly executes the recompute the prior entry committed to, on the first attempt. | **None of H3/H4/H5's confirmed/not-confirmed status changes from the original 4-pair result — H3 and H5 remain NOT CONFIRMED, H4 remains CONFIRMED — but the pooled point estimates move toward a more favorable reading for the method on both H3 and H4, and a less favorable one for H5's precision (its CI now sits entirely above zero, even though the upper bound still fails the equivalence bar).** This is reported as a genuine mixed result, not smoothed into either "generalizes" or "doesn't generalize." **This remains a PRELIMINARY pooled number**: 6 items are still pending adjudication, and per this table's entry on the new pairs' own §6 metrics, T5 (moved guidelines) independently dropped to 0% precision on these same two pairs — a weakness this pooled H3/H4/H5 re-run does not itself surface (T3 is what H4 tests, not T5), but which any paper draft citing this pooled result must still disclose alongside it, per that entry. Should be re-run once the 6 pending items resolve; a 6-item shift is unlikely to flip either CI's zero-crossing given their current width, but should not be assumed rather than checked. Full account `FEASIBILITY.md` §92. |

| 2026-08-24 | **`NewPairs_Adjudication_6_items_COMPLETED.xlsx` returned: all 6 pending I/J disagreements resolved.** `run_new_pairs_metrics.py`'s `ADJUDICATION_FILE` hook picked it up automatically (no code change), matching the identical pattern already proven for H3′. Tennessee's 3 (all G/H-style NONE-vs-CANNOT_DETERMINE splits) resolved to NONE; Connecticut's 3 resolved to real item-ids (a split, a moved, and a reworded correspondence) rather than NONE or CANNOT_DETERMINE — a different, more informative pattern than every other adjudication set in this study, where the disagreement usually collapsed to one of the two "no correspondence" categories. Full 120-item ground truth (116 usable, 4 genuinely CANNOT_DETERMINE) now complete, zero items pending. **Recomputed section 6 metrics, pooled**: cannot_determine_rate 3.33%, correspondence accuracy raw 62.07%/weighted 83.97%, provenance_loss_rate weighted 3.34%, false_correspondence_rate weighted 13.66% — all within ~2 points of the interim 110-item numbers reported two entries above, confirming the interim reading was not sensitive to the 6 pending items as anticipated there. **T5 (moved guidelines) remains exactly 0% precision, now on the complete n=20** — the interim finding is not an artifact of partial data. Full report `annotation_packets/new_pairs_final_metrics.json` (overwritten, superseding the 110-item interim numbers). Code changed: none — the fix that makes this automatic was already committed. `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Directly executes the follow-up the pending-adjudication entry flagged, on the first attempt. | **This is now the final, complete section 6 result for the fifth/sixth confirmatory pairs — no items remain pending anywhere in this study's 6-pair annotation effort** (H3′'s 5, and these 6, both now fully resolved). The formal H3/H4/H5 6-pair re-run (the entry documenting the pooled hypothesis-test result, above) was itself run against the 110-item interim ground truth and is being re-run now against this final 120-item ground truth — see the following entry for that result. `FEASIBILITY.md` §90 is left in place per append-only discipline; this entry and a forthcoming FEASIBILITY.md subsection carry the final numbers. |

| 2026-08-24 | **Final H3/H4/H5 6-pair re-run against the complete, fully-adjudicated 349-item ground truth (was 343, preliminary, two entries above): no confirmation status changes from either the original 4-pair result or the preliminary 6-pair result. Pooled: H3 NOT CONFIRMED (item-raw point −0.0172, 95% CI [−0.0602, 0.0258]), H4 CONFIRMED (T3 precision 97.67%, unchanged), H5 NOT CONFIRMED (item-raw point 0.0506, 95% CI [0.0031, 0.0976] — still exceeds the +0.05 equivalence bound).** New-2-pairs-only breakout (n=116, was 110): H3 item-raw point +0.0431, CI [−0.0259, 0.1207] — still crosses zero, NOT CONFIRMED. **H3 pair-raw shows CONFIRMED: True (point +0.0431, CI [0.0179, 0.0667]) — investigated before reporting, per §10, given it is both surprising (favorable) and the first "confirmed" reading H3 has had anywhere in this study.** Checked directly: Tennessee-alone diff = +0.0667 (n=60), Connecticut-alone diff = +0.0179 (n=56) — both individually positive. With only 2 pair-units to resample from, pair-level bootstrap has exactly 3 possible outcomes (Tennessee-alone, the pooled pair, Connecticut-alone), each already computed and each already positive — so the "95% CI" trivially excludes zero not because of genuine sampling evidence at scale, but because there is no possible resample that isn't positive when both of only two available units already are. **This is a bootstrap-support artifact of n=2, not real statistical confirmation of H3** — exactly the caveat `run_full_comparison_6pairs.py`'s own docstring pre-registered ("pair-level CI with only 2 pair-units, too few for a meaningful pair-level bootstrap, reported for consistency only") before this number was ever computed. Full report `annotation_packets/full_comparison_6pairs_report.json` (overwritten, superseding the preliminary 343-item numbers). No code changed; `item_align.py`/`item_parser.py`/`edition_align.py`/`corpus_probe.py` unchanged at `d3068ee`. | Directly executes the recompute the pending-adjudication resolution made possible, and applies §10's standing scrutiny to the one genuinely new, favorable-looking number this recompute produced. | **This is now the final, complete formal H3/H4/H5 result for the full 6-pair, 360-item confirmatory dataset — nothing remains pending anywhere in this study's annotation effort.** No confirmation status differs from the original 4-pair result: H3 and H5 remain not confirmed at the pre-registered significance bar; H4 remains confirmed and, if anything, strengthens. The apparent H3 pair-level "confirmation" on the new-pairs-only breakout must NOT be reported or cited as evidence for H3 — it is disclosed here specifically to prevent that misreading, not as a finding to celebrate. `FEASIBILITY.md` §92's text is left in place per append-only discipline; the final numbers and this artifact's explanation are added as new subsection §92.5. |

## Appendix A — Reproduction

```
cd sop-guard/backend
python -m app.research.cross_edition.corpus_probe  <edition.pdf>
python -m app.research.cross_edition.item_parser   <edition.pdf> --json items.json
python -m app.research.cross_edition.item_align    <old.pdf> <new.pdf>
```

## Appendix B — Known weaknesses at registration

Recorded so they cannot be presented later as discoveries.

1. **21.1% of items are unmatched on the dev major pair**, mixing genuine
   deletion with residual parser failure. The annotation in §5 exists partly to
   decompose this; until then it is reported, never claimed as deletion.
2. **Two guidelines never parse a title** in any NASEMSO edition
   (`<untitled@…>`).
3. **Guideline matching is permissive by design** — containment-biased overlap
   paired `Cyanide Exposure` with a truncated `Exposure`. A manual audit of all
   accepted guideline pairs is required before publication.
4. **Offsets carry a 3–4% mismatch tail** against `canonical_text`,
   uninvestigated.
5. **Sections yielding no items** (~160–170 per edition) have not been checked
   for whether any contain real recommendations.
